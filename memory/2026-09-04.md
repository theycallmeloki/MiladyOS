## F4/F5 seam: sandman-as-CLI — deployed live
- symphony image ee87142780f1 LIVE (pod symphony-bf89cd9cb), sandman v0.2.50 baked, CLI reaches .15 daemon via --addr (scheme-stripped).
- sandman CLI: `delta` verb + `job list --input-commit` (v0.2.50, fleet rolled). symphony Sandman module (System.cmd, SANDMAN_CLI overridable for tests); BuildFusion jobs + RepoDelta emit + TrackedRepos on CLI; registry tag check stays HTTP (registry's own API); bootstrap tree materialization stays on sandman file GETs (bulk). 405 tests / 2 known flakes.
- DEPLOY WRINKLES hit (all fixed + committed):
  1. Symphony image build (kaniko, in-cluster) cannot reach github.com (404) → moved sandman CLI fetch to internal-registry image `sandman-cli:v0.2.50` COPY --from (Dockerfile 25b3d24). Builds egress Docker Hub + internal registry only.
  2. buildbus promote runs in alpine/git (NO curl) → registry-tag guard with curl broke every promote. Then wget 404'd via step proxy → guard REMOVED as redundant (kaniko-build succeeding already guarantees the tag exists; buildbus exits before promote if kaniko fails). Keep the identity guard. buildbus config commits 33bfb82→54de23b→63227fd, synced to gitea via buildbus-sync.sh.
- wp log API: /api/repos/{id}/logs/{pipeline}/{step_id} (NOT wf in path), base64 per-line. wp.tok lived in /tmp (gone) → re-fetch via `docker exec miladyos grep WOODPECKER_TOKEN /var/lib/woodpecker/.secrets`.
- ArgoCD sync lags → patch websites-private to HEAD each deploy; first sync can hit pre-promote rev (OutOfSync) → re-nudge.
### Seam live-test (in-pod, deploy/symphony, image ee87142780f1) — PASSED
Executed the exact argv the Sandman module builds, against the live daemon from the pod:
- BuildFusion jobs query: `job list symphony-watch --input-commit ee87142780f15ad8 --json` -> the watch job (state success, inputCommits=[head]); non-matching commit -> `[]` (server-side filter proves F5; no wide window + envelope matching).
- TrackedRepos: `pipeline list --json` -> symphony-watch carries name/state/input.git.url/branch (reconcile populates git_url).
- RepoDelta delta: stale-base payload -> `{"applied":false,"reason":"delta base ... does not match mapped head (recorded ee87142780f15ad8)..."}`, EXIT 0 (applied=data). No mutation/build. Report matches RepoDelta.confirm_delivery's not_applied path.
- App logs clean (no sandman/build errors); /api/v1/state HTTP 200.
NOTE: exercised the CLI invocations + parse (module argv validated by sandman_test stub). NOT yet driven from inside the BEAM via a symphony-orchestrated deploy (Deployer->RepoDelta.emit_delta->BuildFusion) — that's the next-level live test if wanted.
### OPS port roll-to-completion: address-parse — validates the CLI seam E2E
Ran the full OPS.md runbook (A-J) on templates/address-parse.Jenkinsfile to validate the new CLI-backed sandman client via a REAL symphony deploy.
- A extract (mode A embedded). Extract had a groovy double-escape SyntaxError in main.py regexes -> collapsed \\\\ -> \\ (extractor artifact), probe green 5/5.
- B wire address-parse-watch (repo address_parse, PROMOTE=0). C seed -> baseline image address-parse:667549c951e7.
- D harness intent int-1788468133475149-O6-mXA (job-port,no-verify): pi pass parked ~3min awaiting. NOTE: /api/v1/intents shows "open" while orchestrator /state shows it "running" — open==processing. Pi DID substantive main.py work (+171/-122: dup dict keys removed, unit-vs-city disambiguation, expanded street types). GAP: pi did NOT deepen probe.py (still 5 cases) nor write done.txt despite the brief (result status "completed"). 
- E review. F DEPLOY = THE SEAM TEST: RepoDelta.emit_delta via new Sandman.delta (CLI) emitted workspace delta -> mirror 13f1106fd488dda1, main.py landed, watch->buildbus -> address-parse:13f1106fd488. BuildFusion reconciling via CLI. PASSED.
- G register job pipeline (image 13f1106fd488, probe-gated; created address-parse-tick repo). H trigger -> job success, real result.json parse (123 Main St Apt 4B Springfield IL 62704 -> number/street/unit/city/state/zip). I close (done). J auto-promote once -> ports/address-parse/ + PORTS.md row + git rm Jenkinsfile, commit 9f85e70.
Verdict: the CLI seam works end-to-end in the live BEAM (emit_delta + job query through sandman CLI). Watch NOT running this session (was via omp hub earlier) — promoted manually.
### F2: buildbus pipeline multi-home drift — reconciled + documented
The buildbus def had 4 homes w/ drift. Real risk: MiladyOS woodpecker/build-bus.yml (product builtin, seeds fresh instances/ISOs) carried the PRE-hardening promote (unguarded identity -> would re-seed the historical mis-promote hole). Cause: buildbus changes were historically authored in MiladyOS (24a7e81/e1c861f/f0d8065), but the 2026-09 deploy-chain hardening landed only in sandman-pipelines/buildbus + gitea live, so the builtin silently fell behind.
Fix (correct under any ownership model):
- MiladyOS woodpecker/build-bus.yml now byte-identical to canonical sandman-pipelines/buildbus/.woodpecker.yml (md5 f796e224; was 151-line pre-hardening). Commit 4dc649f.
- kaniko tag: builtin kaniko-build.yml already at kaniko-submit:2 (product intent, 24a7e81); live gitea milady/kaniko was at :1 (only diff). Aligned live to :2 (sha bead3af) -> live == builtin. :2 proven in prod (live buildbus runs it).
- scratch-build.yml: no persistent forge repo (Phase-A cli-exec) -> nothing to sync.
Ownership model adopted (governance): ONE author per artifact; all other copies derived + byte-identical + never hand-edited. buildbus author = sandman-pipelines/buildbus (deploy-chain; hardening/OPS/F1 there); derived = gitea milady/buildbus + MiladyOS build-bus.yml. kaniko/scratch author = MiladyOS woodpecker/; gitea milady/kaniko derived. Doc'd in woodpecker/README.md "Pipeline ownership & sync" + sandman-pipelines buildbus/README "Third derived copy". NO cross-repo CI guard (fragile across 4 disjoint stores); doc'd invariant + refresh commands instead (agent/pi reads before editing a derived copy). Pushed both repos.
Note for future: independent pi/ACP agents editing a materialized copy (gitea/sandman-store/product-builtin) in place is the drift mechanism to forbid; they must edit the author home + run the sync.
### Architecture decision: pi/blast-radius boundary (with loki, ~03:00)
Resolved the "github-gate the buildbus fetch?" question. Verdict: NO guard now — acceptable chaos, but a named fence.
- pi's reach = ONLY the sandman-pipelines job-port chain (the ~888 low-surface templates -> ports/ review -> push github). Terminal state is human-reviewed (OPS J: auto-promote.py once, delete .Jenkinsfile).
- Machinery (sandman, MiladyOS) reaches main/releases via github normally; NOT pi-operated.
- symphony is NOT pi-operated: its sandman mirror is fed ONLY by human mirror-sync of a TESTED local working tree -> buildbus -> release+deploy (ArgoCD). The buildbus promote identity guard already blocks non-symphony self-promote.
- The nominal github.com/theycallmeloki/address_parse.git (watch git-input) is 404/nonexistent = trigger-only, cosmetic; job builds actually come from symphony's deliberate dispatch. Defer.
- KEY FACTS: buildbus fetch-context reads build SOURCE from sandman control plane (SANDMAN_ADDR), NOT gitea/github. gitea milady/buildbus holds only the pipeline DEFINITION. Sandman mirrors are NOT live github imports (advance only via pi deltas / host mirror-sync). address_parse mirror head 13f1106fd488 = pi deploy delta, built to container w/o github ever seeing it (proof of the seam).
- FENCE to keep: pi = job-port chain only; symphony + machinery mirrors = human-fed. If a future pi-acp task is aimed at the symphony repo/mirror, add a guard THEN (e.g. buildbus fetch from github main, or dispatch refuses revisions not github-visible). Do not pre-build.
### Frame of reference: full build machinery (MiladyOS/deploy/kaniko + buildbus) — mapped
TWO registries (don't conflate): ogmiladyloki = MiladyOS PRODUCT image (host-built, builder.sh docker build/push ogmiladyloki/miladyos). miladyosregistry.transparentlyrotatableproxy.site (proxy front for in-cluster 192.168.1.202:5000 registry:2.8.3 on 50Gi longhorn PVC, metallb, sandman ns) = SANDBOX build images (port jobs, worker, sandman-cli, kaniko-hook/submit, gh-webhook; + apply/cache = kaniko cache).
In-cluster KanikoBuild fabric (deploy/kaniko/, ArgoCD app "kaniko", sandman ns): CRD kanikobuilds.build.miladyos.io/v1alpha1 (kb) -> metacontroller CompositeController kaniko-build-controller (child=Pod) -> sync hook http://kaniko-hook.sandman.svc:8090/sync -> kaniko-hook:2 Deployment (own SA; was uncontainerized 192.168.1.147:8090 script w/ hardcoded /home/laneone/kubeconfig = SPOF) -> kaniko pod (busybox init materializes context.tar.gz; gcr.io/kaniko-project/executor:latest --context=tar:///workspace/context.tar.gz --destination ... --cache=true --push-retry=3; restartPolicy Never). Terminal latch on observedGeneration (never rebuild once done). Context caps: inline b64 ~128KB argv (MAX_ARG_STRLEN); configmap >~190KB raw busts 256KB client-side-apply annotation -> ALL server-side apply (kaniko-submit:2 = 24a7e81). RBAC kaniko-builder SA + kanikobuilds-admin Role (kanikobuilds CRUD+status, configmaps) bound to kaniko-builder(sandman) + default(miladyos).
Shared submitter seam: kaniko-submit.py (deploy/kaniko + baked kaniko-submit:<tag>/app) used by BOTH buildbus.kaniko-build AND woodpecker/kaniko-build.yml. tar.gz honoring .dockerignore (dir exclusion), registry-skip via tags/list, server-side apply CR (+context configmap), poll phase. Product native kaniko-build.yml submits from CI_WORKSPACE checkout; scratch-build.yml = host-daemon alt (Phase A docker.sock, no cluster). buildbus = fetch-context(sandman SANDMAN_ADDR)->kaniko-build(kaniko-submit:2)->promote(alpine/git, identity-guarded, PROMOTE=1: clone self-hosted-k8s-private sed image commit "sandman build-bus" push -> ArgoCD self-heal). Port jobs PROMOTE=0 (registry only); symphony PROMOTE=1 (deploy).
Egress: kaniko pods reach Docker Hub + internal registry ONLY (not github) -> why sandman-cli ships via internal-registry COPY --from image.
OPEN to pin before relying: which woodpecker env runs the buildbus that submits KanikoBuilds (.147 wp vs in-cluster miladyos-self) + its kubeconfig_content grants sandman-ns submit. Everything else verified from source.
### .147-scoped build execution model (verified live, Talos cluster)
Geographic split (3 machines share the build frame;):
- .147 host = docker miladyos:woodpecker-pilot (gitea :3000 + wp :8000). Repo 3 = milady/buildbus runs HERE (recent wp#61/62/63 success). buildbus kaniko-build uses kubeconfig_content secret to submit KanikoBuild CRs to the cluster.
- 192.168.1.15:4242 = sandman DAEMON (separate box, NOT a Talos node). buildbus fetch-context reads build SOURCE from here (SANDMAN_ADDR).
- Talos k8s cluster = kubectl context admin@miladyos; control-planes .40/.41/.42, workers .97/.126/.160/.168. sandman ns: kaniko-hook@talos-ugl-4xf, kaniko-registry@talos-ga5-yk4 (192.168.1.202:5000), sandman-worker-hook, CRD kanikobuilds + metacontroller composite controller. Kaniko executor pods run on Talos workers. Recent CRs = port jobs (ability-check/accounts-receivable/address-geocode) -> miladyosregistry...:<rev12> Succeeded.
- Registry in-cluster 192.168.1.202:5000, public name miladyosregistry.transparentlyrotatableproxy.site -> (proxy). reachable from .147 wp (skip checks) + kaniko pods (push).
So: .147 = CI/trigger; .15 = sandman control plane (build source); Talos cluster = kaniko execution + registry + ArgoCD deploy target.
### kaniko-builder consolidation (retire data-plane bus; keep one in-cluster front-door)
Per laneone: one build executor (MiladyOS/deploy/kaniko CRD/controller/hook/kaniko-submit.py) shared by ALL KanikoBuild triggers -> the standalone sandman data-plane kaniko-builder bus was a SECOND front-door, now retired.
- Moved templates/kaniko-builder/ (sandman-pipelines) -> MiladyOS/deploy/kaniko/kaniko-builder/ (co-located with machinery it drives). MiladyOS commit 19c7ef5 (pushed). STATUS header marks it RETIRED 2026-09-04, preserved as def (spec + Dockerfile kaniko-builder:3 + runner.sh + seed-example).
- Deleted sandman-pipelines templates/kaniko-build.Jenkinsfile (dead Jenkins-era KanikoBuild front-end, no Jenkins in stack). sandman-pipelines commit dfb0549 (pushed).
- Retired live sandman pipeline: DELETE /api/v1/pipelines/kaniko-builder?keepRepo=1 (HTTP 200). Verified gone; builds input repo + kaniko-builder output repo PRESERVED -> re-instantiable from moved spec.
- Remaining build front-doors: woodpecker buildbus kaniko-build stage (in-cluster, deploy path) + woodpecker scratch-build (host daemon, ad-hoc). One executor underneath.
### MiladyOS MCP tool healthcheck + write_file perms fix (IMMEDIATE only)
Located MCP: mcp.transparentlyrotatableproxy.site -> proxy -> ingress deploy/miladyos-mcp -> external svc 192.168.1.147:6000 = THIS box. SSE server = python pid in /miladyos docker container (host-network, binds :6000), launched startup.sh `python -m main mcp`, HOSTNAME=miladyos-42.
Healthcheck: hello_world/get_milady_time/get_divine_rng/list_evolution_goals/list_evolved_templates/list_pipelines/pipeline_status/pipeline_logs/read_file ALL work. list_pipelines needs owner/name (milady/buildbus).
- get_divine_rng VERIFIED GENUINE: TempleOS kernel pid 277 running in container (/opt/templeos/templeoskernel under milady_oracle.py pid 273); mcp handler miladyos_mcp.py:1040 sends RNG_REQUEST over TempleOSBridge stdio, parses RNG:<int> from loader stdout; NO random.*/urandom fallback anywhere in mcp. Value came from live kernel RandU64 (hardware-timing). get_milady_time "Milady 3rd" = container UTC clock (2026-09-03), not a bug.
- write_file/edit_file BROKEN -> root cause: /app in container was root:root 755, MCP runs uid 1000(milady); all file tools hardcode FSPath("/app")/path (miladyos_mcp.py:698/711/727/753). FIXED (immediate): `docker exec -u 0 miladyos chown -R 1000:1000 /app`. Verified write->read->edit roundtrip via MCP tools.### AlphaEvolve: mechanical EVOLVE-BLOCK fence (restored from history) + first real target
User pointed out full git history present (695 commits, not shallow) -> found predecessor miladyos_evolve.py (deleted be9820f in jenkins-word arc) HAD the real mechanical fence: extract_evolve_blocks/replace_evolve_blocks; current alpha_evolve.py had REGRESSED to soft prompt-only ("keep markers intact", no enforcement). Fitness is static regex heuristics + YAML gate; live execution effectively NEVER runs (gate evaluator.live_execution referenced once, defaults False, no config sets it; config's cascade execution weight 0.7 is dead). Container /app/templates EMPTY (no live corpus); /app/evolved_templates empty.
DECISION (user): harden the marker into a real mechanical fence NOW, shaped C (one real target). Chosen target = woodpecker/kaniko-build.yml (the canonical build-only seam; NOT build-bus/deploy machinery per prior posture).
- evolve_fence.py (NEW, pure stdlib): parse_blocks (markers -> spans, payload = lines strictly between; unclosed/nested -> issues) + apply_fence(template,candidate) = clamp candidate into byte-immutable skeleton: adopts ONLY candidate payloads (matched by marker order), rebuilds skeleton+marker lines verbatim from template; candidate that edits/drops/reorders outside loses those edits; no-marker template -> pass-through (legacy).
- alpha_evolve.py wired: import evolve_fence; self._fence_template set in evolve(); engine._enforce_fence() clamps candidate; applied at BOTH mutation sites (initialize variants + evolve-populations offspring); prompt req 6 strengthened (only lines between markers may change).
- woodpecker/kaniko-build.yml: authored EVOLVE-BLOCK-START/END (# comments in the commands block scalar) around the movable surface = KANIKO_TIMEOUT default + kaniko-submit flags; rest (kubeconfig write, from_secret, image) protected. Still valid YAML.
- test_evolve_fence.py (13 asserts): in-payload edit kept; outside edit (image/mkdir) clamped back; skeleton byte-identical; dropped marker -> original payload; multi-block; no-marker legacy; marker-metadata drift not adopted. PASSED host + container /app/.venv.
- Real-file dry run: candidate tuning timeout 1500->1200 + tampers (image :2->:9, mkdir /tmp/evil) -> clamped to payload-only diff. 
- Docs: added "Mechanical enforcement (evolve_fence)" under alpha-evolve EVOLVE-BLOCK Markers.
NOTE: changes are SOURCE (MiladyOS repo), NOT yet deployed to /app (live MCP still old alpha_evolve). Known gap left as-is (pre-existing): fitness is static heuristics, live_execution off — fence makes markers safe; behavioral fitness is a separate future slice.
### AlphaEvolve fence + behavioral fitness DEPLOYED & VERIFIED (commit 7545171 "looks good to milady")
B done + evaluated: copied alpha_evolve.py + evolve_fence.py into container /app (backup /app/alpha_evolve.py.bak); seeded /app/templates/kaniko-build.yml (marked target). Deployed-engine eval PASS: tampered candidate (image :2->:99, mkdir /tmp/pwned, TIMEOUT 1500->750) clamped to payload-only — protected image+mkdir kept, tune adopted, log "1 block(s); adopted edits in 1 payload(s)".
Behavioral fitness slice: exposed evaluator.live_execution (default false, documented in configs/evolve_default.yaml) — real per-candidate wp execution via WoodpeckerClient.run_content (pushes .woodpecker.yml to forge repo milady/evolve, triggers, polls). Baked configs/ into /app/configs (FIXED live load_config() crash: /app/configs was absent from image -> live evolve_template would have FileNotFoundError'd). ONE live test PASS: side-effect-free echo candidate, live_execution on, real wp run (4s), success_rate 1.0, duration_score 0.987, execution_skipped 0. Live fitness loop works when opted in.
DEPLOY CAVEAT: the running MCP process (pid 7570, python -m main mcp) imported alpha_evolve at startup -> still holds OLD code in memory. /app files are updated for the NEXT process start; to use the new fence/fitness via the HTTP MCP path a process restart is needed (not done). Behavioral live_execution intentionally OFF by default; only enable for cheap side-effect-free candidates (kaniko-build candidates would run REAL image builds).
### MiladyOS container redeployed to canonical CI image (ogmiladyloki/miladyos)
Resolved woodpecker-pilot confusion: PR #1 was the Jenkins->Woodpecker migration ANALYSIS (closed, analysis-only). No woodpecker-pilot branch exists — main IS the woodpecker codebase; miladyos:woodpecker-pilot was a local image tag of main@Sep2; ogmiladyloki/miladyos:latest was stale pre-woodpecker. Decision (user): promote main rebuild as canonical; direct CI recreate (NO backup), accepting .147 wp/forge writable-layer loss (gitea milady/buildbus + milady/kaniko defs + wp history gone -> re-seedable from author homes per ownership docs).
SYMPHONY UNAFFECTED (verified): symphony uses its OWN in-cluster redka (deploy/redka, nalgeon/redka, /data/redka.db on redka-data-pvc longhorn), REDKA_ADDR redka.default:6379. Not the miladyos container's redis. Also miladyos image now boots its own redka (log "starting redka") - separate.
Dockerfile fix (0a18ecf, pushed): added evolve_fence.py to /app python COPY; created+chowned /app/configs; baked configs/evolve_default.yaml (fixes live load_config() crash). CI (github Actions) builds main on push -> ogmiladyloki/miladyos:latest + ISO/version.sh tag. 0.0.0.0.645 pushed 23:42Z = the 0a18ecf fixed build.
Redeploy: docker pull ogmiladyloki/miladyos:latest; verified baked evolve_fence.py + configs + alpha_evolve imports (load_config OK, live_execution=False); stop/rm old container; docker run (builder.sh flags: --privileged --user root --net=host, docker.sock, MILADY_ADMIN_ID/PASSWORD). MCP up on :6000, SSE endpoint streaming, hello_world OK. Running container now runs the canonical image with the mechanical fence live (no more hand-copied /app).
NOTE: Docker Hub image public (anonymous pull). GitHub API rate-limited from this proxy IP -> detect CI completion via hub.docker.com tags API instead.
### .147 forge/wp re-seed + builder.sh run-user fix (container already on canonical image)
Canonical image boot gap found: forgejo REQUIRES running as milady (startup.sh app.ini RUN_USER=milady); builder.sh's --user root made forgejo refuse -> "forgejo did not become ready - Phase B skipped" -> no forge/wp. Also current image's milady lacks the docker group (old pilot had gid 981). FIX: container recreated as milady + --group-add 981 (host docker gid) -> forgejo :3000, woodpecker-server :8000, agent (miladyos-42, docker backend) registered, MCP :6000 all up.
Re-seeded forge repos from author homes (byte-identical verified): milady/buildbus .woodpecker.yml md5 f796e224 (== sandman-pipelines/buildbus/.woodpecker.yml), milady/kaniko .woodpecker.yml md5 cacc6ca (== MiladyOS woodpecker/kaniko-build.yml incl. inert EVOLVE markers); both activated in wp (buildbus id 1, kaniko id 2) via forge POST + contents API + wp /api/repos?forge_remote_id. Method recorded (create_pipeline mechanics in miladyos_mcp.py: forge_create_repo -> forge_upsert_file -> repo_id activation).
builder.sh patched (a1a2eef, pushed): run container as milady (image default USER) NOT root; --group-add $(stat -c %g /var/run/docker.sock || echo 981). Prevent future forgejo Phase-B skip on builder.sh redeploys.
### woodpecker/ housekeeping + MiladyCI facade (MCP job_* tools) - commit 979d8e7
User-driven organize: move woodpecker_client.py into woodpecker/ package + add milady abstraction so MCP tools never expose gitea. Design settled with user (multiple ask rounds):
- yml contract = plain steps yml (image+commands), envelope (when: event: manual) auto-injected.
- surface = NEW job_* verb set (job_define/job_run/job_status/job_logs/job_list); retired create_pipeline/run_pipeline/pipeline_status/pipeline_logs/list_pipelines (facade-only cutover). execute_command kept, now -> MiladyCI().execute (asyncio.to_thread).
- routing = catalog + explicit (NO content sniff): KNOWN_JOBS kaniko-build->milady/kaniko, build-bus->milady/buildbus are kaniko; new jobs = node agent. bare name auto milady/<name>; owner/name passthrough.
- deploy workflow = iterate in-container first (done: copied layout over /app, restarted MCP, live facade smoke green: define milady/hello run #1 success, status/logs/list, unknown-job friendly err, execute success), then commit + canonical rebuild+recreate.
Files: woodpecker/{__init__,woodpecker_client,service}.py; miladyos_mcp import `from woodpecker import MiladyCI, WoodpeckerClient`; main/alpha_evolve/meta_evolve imports -> package. Dockerfile:185 flat COPY dropped woodpecker_client.py, added COPY woodpecker/.../service.py /app/woodpecker/. MCP tool count still 16 (old raw 5 -> new job 5).
IMPORTANT: recreate (this rebuild) wipes .147 writable layer again -> re-seed milady/buildbus + milady/kaniko forge repos after container is up (forge repos + wp activation live in /var/lib/{forgejo,woodpecker} writable layer). Facade known jobs need those repos seeded to run.
### RESULT: canonical rebuild 979d8e7 deployed on .147 (2026-09-04 ~08:37Z)
GitHub Actions built 979d8e7 -> ogmiladyloki/miladyos:latest (docker hub last_updated 08:36:10Z, repo digest 3b967d5b). Pulled (5.8GB), container recreated with fixed flags (--user milady --group-add $(stat -c %g /var/run/docker.sock)=981 --privileged --net=host + env + docker.sock). Verified fresh: forge :3000 200, wp :8000 200 + agent miladyos-42 (docker), MCP python up; /app/woodpecker/{__init__,service,woodpecker_client}.py baked, flat /app/woodpecker_client.py removed; MCP serves job_* tool set. Re-seeded forge repos milady/buildbus (id1) + milady/kaniko (id2). Live MCP: hello_world "milady!", execute_command ran ad-hoc pipeline success (via proxy ingress). MiladyCI roundtrip on fresh stack: define milady/hello (node) run #1 success; backend map kaniko-build/build-bus=kaniko, new=node. Demo jobs milady/hello + milady/ad-hoc left on fresh forge (harmless).
### E2E spin: node-agent + kaniko builder via MCP job_* tools (sandman-pipelines/ports/dice-roll)
Question answered: ports are NOT ingested via read_file-into-yml. Port source (Dockerfile/main/probe/spec) lives in sandman-pipelines/ports/<name> and enters an IMAGE via container build; the published image carries the source. The probe job referenced the already-published image + ran its /app/probe.py. Kaniko BUILD path = a forge repo IS the build context (Dockerfile at root); kaniko-submit ships the context (inline base64, .dockerignore honored) as a KanikoBuild CR into the sandman namespace and pushes.
Node path (MCP): job_define("dice-roll", steps referencing dice-roll:2f21f5a1827e image + run probe.py) -> job_run #1 -> probe PASSED (120 checks), job_status/logs/list all green.
Kaniko path (MCP job_run): made milady/dice-roll a build-context repo (pushed Dockerfile/main/probe/spec/.dockerignore + .woodpecker.yml = woodpecker/kaniko-build.yml), attached wp repo secret kubeconfig_content = host ~/.kube/config (admin@miladyos, wp secret API needs events:[...] else 422; secret value never returned by GET - masked). job_run("dice-roll", {DESTINATION: miladyosregistry.../dice-roll:51c22930633f, KANIKO_TIMEOUT:900}) #2 -> KanikoBuild CR kb-dice-roll-latest-20260904085038 Succeeded in sandman ns; image tag 51c22930633f now in registry. Cluster fabric alive (CRD kanikobuilds.build.miladyos.io; host kubectl admin@miladyos).
NOTE: repo secret is the FULL admin kubeconfig (broad); it + milady/dice-roll kaniko context live in forge/writable layer -> wiped on next builder.sh recreate (re-seed source+secret to re-run kaniko jobs). Cluster/registry live outside container.

### Decision: native Go `milady` companion replaces Python mcp-llm-bridge + host ops
- Grounded: go-mcp has caught up (mark3labs/mcp-go full spec: client SSE + Streamable HTTP, active ~monthly; openai-go official handles OpenAI-compatible completions + tools). Python BridgeManager window closed.
- `milady` becomes the HOST-RESIDENT companion binary (cobra/spf13), owning host-side ops the container can't: `slurp` (build-context tar.gz -> /upload), `k3s join` (ISO token discovery+registration). Container = control plane. Bridge `ask` role subsumed.
- Slurp core: ONE ignore-honoring folder path (git repos ride free by design — working tree honors .gitignore/.dockerignore, so 77GB untracked junk excluded).
- MiladyOS/milady = Go module (github.com/theycallmeloki/MiladyOS/milady). Cobra v1.10.2, Go 1.27.1 (installed ~/.local/go, tarball no sudo). Tree: ask/slurp/k3s join (wired, not-implemented) + version (ldflags).
- Version = 5-octet scheme version.json prefix + git commit COUNT (e.g. 0.0.0.0.647) — matches ISO/version.sh; NOT short hash. GH workflow will derive at build.
- Dockerfile: dropped milady-llm-bridge uv-install block (build-only, nothing ran it); MILADYOS_VERSION kept (CI build-arg metadata). TODO later: real slurp/ask/k3s join, GH native-release workflow (0.0.0.0.<count>), container MCP Streamable HTTP transport option.
- Committed 7ef57ad, pushed to main. Go local in ~/.local/go/bin (1.27.1).

### milady binary release workflow LIVE (verified)
- .github/workflows/milady-release.yml: per main commit builds linux/amd64+arm64 (milady/, -trimpath, internal/version ldflags baked 5-octet) + .sha256 sidecars, gh release create/upload with changelog. Trigger = push main (not tags, unlike sandman) — matches commit-count versioning.
- First release verified: ebb3cc5 -> GitHub Release "milady 0.0.0.0.650" with milady-linux-{amd64,arm64}(+.sha256); workflow completed success. docker_build_push also runs per commit (image tag same 5-octet) — both fire on push to main.

### milady self-update LIVE (verified against real GitHub)
- Ported theycallmeloki/sandman update.go -> milady/internal/update (5-octet compare, milady-<os>-<arch>[.sha256] assets, MiladyOS repo, atomic temp+rename over /usr/local/bin/milady, sudo re-exec, MILADY_UPDATE_REEXEC). `milady update [--check]` cobra command. Dev builds (unparseable version) bootstrap onto latest release.
- GitHub serves latest-release assets at .../releases/latest/download/milady-<os>-<arch>[.sha256] -> curl bootstrap works, no API/jq.
- Live-verified: behind/current/equal branches vs real GitHub; shipped release 0.0.0.0.652 (commit a713d00) carries `update`, self reports "up to date". sha256 contract OK. Unit tests (octet compare, checksum, httptest e2e) green.
- NOTE: milady/install.sh added (curl bootstrap, no systemd, INSTALL_DIR override) — verified to temp dir; `curl -sSL https://raw.githubusercontent.com/theycallmeloki/MiladyOS/main/milady/install.sh | sh` is the bootstrap. Committed 9d7c862.
