### Fresh CI image launched + 2 kaniko-path bugs fixed (8eceefe)
- Launched ogmiladyloki/miladyos:latest (CI image 2a62f53516e9) via builder.sh-style run + MILADY_KUBECONFIG_CONTENT=(host ~/.kube/config, admin@miladyos) env so import_context auto-attaches kaniko secret. Self-bootstraps: forgejo:3000, wp:8000+agent, MCP:6000 (/upload live), docs:8081. startup.sh "server/agent did not both start" warning is spurious pgrep bug — both processes alive.
- Real bugs (only visible now kubeconfig auto-attach path is live):
  1. repo_secret_set: wp GET /secrets returns null (not []) on empty -> 'NoneType' not iterable. Guarded json() iteration sites x3.
  2. Secret POST 422 "no event specified"; v3.18 rejects 'deploy' too -> events ['push','tag','pull_request','manual'].
- Hot-patched woodpecker_client.py into running container (writable layer) + restart to validate. MCP-driven E2E verified on fresh image: milady slurp /tmp/omsmoke --push -> import kaniko:true kubeconfig_secret:true; job_run(omsmoke, DESTINATION=...omsmoke:boot) -> pipeline #2 success; registry tag omsmoke:boot present.
- Lesson: drive builds via MCP job_* tools, not raw in-container python (user corrected).
- STATE: fixes committed but only hot-patched in running container; Docker Hub :latest still buggy-baked. Next manual image build bakes 8eceefe -> pull -> recreate for clean state (wipes omsmoke test repo etc).
### Clean baked image flow working (builder.sh pull path)
- docker_build_push manual run baked 8eceefe+ into :latest / 0.0.0.0.669 (digest a110d8bf). GH cache-to export (#77) runs after the Docker Hub tag push (#75) -> job "in_progress" lags; image pullable as soon as tag lands.
- builder.sh rewritten: default docker pull :latest + run (no local build); --local / --local --fresh variants; passes MILADY_KUBECONFIG_CONTENT from ~/.kube/config at launch. Verified: pull+launch clean.
- 3rd fresh-container bug: WOODPECKER_TOKEN missing because token() cached "" on an early pre-dance call ('if self._token is None' never re-read an empty string). Fixed to re-read whenever empty (committed; next image build bakes it). This boot: token file already present after restart -> import works baked.
- Clean baked E2E proven via MCP: slurp bootdemo -> import kaniko:true kubeconfig_secret:true -> job_run -> pipeline#1 success -> registry bootdemo:bake present.
- NOTE: running image has the token() fix NOT baked (it worked due to token file existing pre-first-call). Next build_all.sh --image bakes it.
### Launched token-fix baked image (digest 24ac5e74) - verified
- builder.sh pull+launch clean. All fixes baked (token re-read / events-scoped / json guards).
- One startup race on first import: .secrets gets AGENT_SECRET etc first, WOODPECKER_TOKEN appended LAST (startup.sh:615) -> a too-early first call saw file present but no token. token() re-read fix absorbs it (retry succeeds). Readiness check should grep ^WOODPECKER_TOKEN= not just [ -s ].
- E2E verified via MCP: slurp verifydemo -> import kaniko:true kubeconfig_secret:true -> job_run pipeline#1 success -> registry verifydemo:latest present.
### Emacs-as-MCP (both client+server) — found + PROVEN in this container
- Found the original: miladyos-extra-deploys/deploy/gotty-dev/emacs-mcp-configmap.yaml (moved 59b2cbd from this repo). It IS both client+server in the evolved version (emacs = MCP server + MCP client). NOT deployed (gotty-dev ns decommissioned).
- Ported to the host-mode container (Debian 13): installed emacs-nox 30.1 via apt (writable layer), wrote /home/milady/.emacs adapted for local 127.0.0.1:6000/sse + dropped cluster DNS/gptel.
- API drift discovery: old configmap used mcp-server-register-tool + mcp-server-start :transport 'sse — REMOVED in current mcp-server-lib (laurynas-biveinis/mcp-server-lib.el, straight-cloned). Current = stdio-only to an emacs daemon; tools registered via mcp-server-lib-register-server :tools ((HANDLER-FN :id ... :description ...) ...); input schema auto-derived from handler arglist + docstring "MCP Parameters:" section (string args, required unless &optional, no &rest); handlers called positionally.
- Driver: emacs-mcp-stdio.sh (in the straight repo) bridges stdio <-> emacsclient <-> daemon via mcp-server-lib-process-jsonrpc. Needs mcp-server-lib-start first ("No active MCP server" otherwise) -> added (when (daemonp) (mcp-server-lib-start)) to .emacs so daemon auto-readies.
- PROVEN: emacs --daemon in container; wrapper handshake returns serverInfo "MiladyOS Emacs" 1.0.0 + 4 tools (emacs-eval/find-file/buffer-content/save-buffer) with schemas; tools/call emacs-eval persists state across calls (setq 42 -> read back). Fresh daemon auto-starts.
- Constraint: wrapper's emacsclient must reach the daemon => co-located (in-container, or host emacs). For THIS host milady to get emacs as a NATIVE harness MCP tool: install emacs+daemon on HOST + add stdio entry to an .mcp.json (tools surface at session start). Container daemon is drivable now via docker exec < wrapper.
- Client half (emacs -> miladyos MCP) configured via mcp.el; try in gotty (http://127.0.0.1:8088, milady:milady) -> emacs -> C-c m l/C-c m t.
