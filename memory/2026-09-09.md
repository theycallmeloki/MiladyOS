# 2026-09-09

## Text-based installer research (ISO) — d-i vs Calamares

Operator: manual Calamares ISO install was rough "because of 3d"; wants a text
installer, ideally for cleaner automated QEMU testing. Asked for searches:
text Calamares, or any upstream Debian text approach.

**Findings (verified from upstream sources, not vibes):**
- **Calamares has NO text/headless/TUI/batch mode.** trixie `calamares 3.3.14-1`
  is Qt6/QML GUI; full CLI = `-h -v -d -D -c -X -T`. Upstream declines headless.
- 3D pain = Qt Quick defaulting to RHI/OpenGL. Fix: `QT_QUICK_BACKEND=software`
  + `LIBGL_ALWAYS_SOFTWARE=1` (+`QT_OPENGL=software`) in `live-installer-xinitrc`.
- **Upstream Debian text path = d-i + live-installer + preseed.** live-build
  flags: `--debian-installer live --debian-installer-gui false`
  `--bootappend-install "console=ttyS0,115200n8"`. Auto-generates "Start
  installer" + Advanced → Text installer → Install/Expert/Automated
  (`auto=true priority=critical`)/Rescue, in both isolinux and GRUB. Preseed at
  `config/includes.installer/preseed.cfg` is repacked into the d-i initrd and
  auto-loaded. Fully headless on serial.
- **live-installer 58 mechanics (source-verified):** mounts
  `/cdrom/live/filesystem.squashfs` and `tar c . | tar x` into `/target` →
  installed node IS the MiladyOS live rootfs (Docker/k3s/firstboot all there);
  purges live-boot/live-config/calamares-settings-debian + update-initramfs.
  GAPS: (1) ISO-fs payload `/cdrom/payload/…` is NOT in the squashfs → not
  copied, needs preseed late_command; (2) no packagechooser → role via
  `milady.role=` cmdline or a TTY prompt.

**Bugs found while tracing:**
- 🔴 `firstboot/ensure-image.sh` looks for `milady-image.tar.zst` but
  build.sh/build-in-container.sh/rolesq stage `miladyos-image.tar.zst` → live
  boots never load the embedded payload, silently fall back to registry pull.
  (git history shows the name drifted.)
- 🟠 `role-detect.sh` interactive prompt is dead under systemd (`[ -t 0 ]` false
  with stdin=/dev/null) → always defaults to agent. Needs a TTY prompt unit.
- 🟡 `MILADYOS_ROLE` set/passed by build.sh but never consumed.

**Wrote:** `ISO/docs/TEXT-INSTALLER.md` — full research, option table
(A: add d-i alongside Calamares + software-render fix, recommended; B: replace
Calamares; C: Calamares-only render fix), concrete change list, preseed skeleton,
sources.

**Next:** operator ruling on A/B/C. Recommend A: land d-i text (automation
unlock) + the software-render fix, then optionally retire Calamares if size
matters. Also fix the ensure-image payload filename + role prompt regardless.

## ✅ Implemented: custom `dialog` text installer (replaces Calamares)

Operator ruling: interactive fun text installer, ASCII art only (no heavy
branding), focused/clean, few steps, everything upfront, disk-focused, hostname
random. Built it (NOT d-i — see docs/TEXT-INSTALLER.md §0 for why).

**New:**
- `ISO/installer/milady-install` — single `dialog` TUI + install engine (~420
  lines). Flow: banner → role → disk → account → (agent token) → confirm →
  gauge install → reboot. Unattended mode: `milady.auto=1 milady.role=…
  milady.disk=… [milady.password=… milady.token=…]` (for QEMU CI).
  Engine: GPT (1MiB bios_grub + 512MiB ESP fat32 + ext4 root), copy live rootfs
  from `filesystem.squashfs`, UUID fstab, node.conf/role-applied cleanup,
  payload → /opt/milady/payload, join-token, operator account, GRUB UEFI+BIOS,
  serial GRUB+kernel console, update-initramfs/update-grub, reboot.
  Disk picker excludes live medium, MILADY_DOCKER scratch, mounted disks,
  zram/loop/ram/dm. Enforces 8GiB min.
- `ISO/installer/ascii-logo.txt` — swappable banner (the only branding knob).
- `ISO/systemd/milady-install.service` — runs installer on `/dev/console`
  (ttyS0 on serial fleet/CI boots, tty0 on VGA), live-only, conflicts gettys.
- `ISO/config/package-lists/miladyos-installer.list.chroot` — dialog, parted,
  gdisk, dosfstools, e2fsprogs, rsync, grub-{pc,efi}-bin, efibootmgr,
  initramfs-tools, kmod.
- `ISO/config/hooks/normal/1400-installer.chroot` — enables the service.

**Removed (gio trash; Calamares GUI retired — no text mode, Qt6/QML 3D pain):**
- `config/package-lists/miladyos-calamares.list.chroot`
- `config/hooks/normal/1400-calamares.chroot`
- `systemd/milady-live-installer.service`, `calamares/live-installer-{session,xinitrc}`
- `ISO/calamares/` modules/branding remain parked (unused) for reference.

**Fixed:**
- `firstboot/ensure-image.sh` payload name mismatch (accepts both
  `miladyos-image.tar.zst` and legacy `milady-image.tar.zst`) — live boots were
  silently falling back to registry pull.
- `firstboot/role-detect.sh` dead prompt: now reads `/dev/console` with a 30s
  timeout, but never prompts on live boots (`/run/live/medium` guard).
- `qemu-install-test.sh`: attaches a throwaway scratch disk as vda so the live
  MILADY_DOCKER store can't grab the install target (vdb); installer is driven
  over the serial socket now (no GUI/sendkey).

**Docs:** `ISO/PLAN.md` D2 + installer layer updated; `ISO/docs/TEXT-INSTALLER.md`
has §0 "what was built" + full d-i research retained.

**Untested:** no docker/ISO build available on this host, so the installer has
only been syntax-checked + parsing-tested. Needs a QEMU install run
(`qemu-install-test.sh out/miladyos-<v>.iso .install-target.qcow2 install`).

## autoresearch × Symphony × sandman — connecting them

Operator: install karpathy/autoresearch one dir above MiladyOS, then wire it so
it's "driven by" MiladyOS; flagged Symphony as a possible orchestrator.

**Installed:** `~/Documents/autoresearch` (cloned, moved out of `~`). Reference
clone `~/Documents/symphony` (theycallmeloki fork of openai/symphony).

**Reachability from workstation `miladyos-42` (192.168.1.147):**
- K8s API `192.168.1.45:6443` + Symphony `:4000` = **unreachable** (host ARP
  FAILED; full /24 scan of 4000/6443 empty). WAN `49.206.252.253` ports 80/443
  closed. Nebula/Tailscale not running locally.
- **sandman UP** at `192.168.1.15:4242` (32 repos, 15 pipelines; `symphony`
  mirrored, `symphony-watch` pipeline failing; autoresearch NOT mirrored yet).
- GPUs on this box: RTX 3090 24G + RTX A4000 16G, 188G RAM, 24 cores. No `uv`
  installed; ollama has llama3.1:8b + nomic-embed.

**How the fork actually works (source-verified):**
- Symphony intents live in redka (`IntentStore`); `POST /api/v1/intents`
  `{intent:{title,description,repo}}`; `:id/assign`, `:id/activate`, `:id/close`,
  `:id/deploy`. Only `open` auto-dispatches.
- `repo` = https clone URL of a repo mirrored in **sandman**. `RepoDelta.bootstrap`
  materializes the mirror head into the workspace; `:id/deploy` emits a git delta
  to sandman `POST /api/v1/git/delta` → new commit on the mirror → re-triggers
  the `<repo>-watch` pipeline. Inert unless `SANDMAN_ADDR` set.
- Agent runtime `pi_acp` spawns `pi-acp` → `pi --mode rpc`; **hardcodes
  `mcpServers: []`** in `session/new` (so no MiladyOS MCP tools to the agent yet).

**Proposed mapping:** autoresearch keep/discard == Symphony deploy/close.
MiladyOS = research director (enqueue intent per experiment, read val_bpb, decide),
Symphony = orchestrator, sandman = mirror + CI, pi = hands, autoresearch = workload.
Needs: mirror autoresearch into sandman + a WORKFLOW.md + MiladyOS bridge tools.
**Awaiting operator ruling** on driver split and permission to install uv /
mirror the repo.

### sandman CLI + worker connected (operator asked)

- Installed `sandman` v0.2.50 CLI to `~/.local/bin/sandman` from the GitHub
  release (sha256-verified). Connected to the existing daemon
  `192.168.1.15:4242`; `SANDMAN_ADDR` exported in `~/.bashrc`.
- Fleet: 8 hosts / 15 pipelines. Repos mirrored: 32 (symphony yes, autoresearch no).
  `symphony-watch` pipeline state `failure`.
- Re-registered this box as worker `miladyos-42` (`192.168.1.147:4343`, labels
  `exec,delta`, GPUs 3090+A4000). Daemon already remembered the name from Sep 6;
  no local unit/binary existed.
- `laneone` is in group `docker` in /etc/group but the login session predates it,
  so the worker launcher uses `newgrp docker` (`~/.local/bin/sandman-worker-up`).
  Persisted as user unit `~/.config/systemd/user/sandman-worker.service`,
  `Linger=yes` (survives logout/reboot).
- Verified execution: worker `POST /exec` runs `alpine` container, exit 0.
  `sandman run <worker>` is wrong (that's the daemon text protocol).
- **GPU passthrough fixed** (operator supplied sudo): installed
  `nvidia-container-toolkit` 1.20.0-1, ran `nvidia-ctk runtime configure
  --runtime=docker` + `cdi generate` (/etc/cdi/nvidia.yaml), restarted docker.
  Verified via worker `/exec` gpus:[0] -> `nvidia-smi -L` = RTX 3090. Docker
  runtimes now include `nvidia`. Sandman GPU jobs on this box work.

### autoresearch running on the 3090 + mirrored into sandman

- `uv` 0.12.11 installed user-local; `uv sync` (torch 2.9.1+cu128);
  `uv run prepare.py` -> `~/.cache/autoresearch` (11 shards, vocab 8192).
- Baseline: **val_bpb 1.327183** (302.9s train, 11.7GB peak, depth 8, 50.3M
  params, 120 steps) after setting `DEVICE_BATCH_SIZE=32`; 128 and 64 both OOM
  the 24GB 3090 (logits buffer). Effective batch (`TOTAL_BATCH_SIZE`) unchanged.
  Branch `autoresearch/sep9`, commit `39769ee`, `results.tsv` untracked.
- Mirrored into sandman: `sandman repo create autoresearch` + `sandman put` of
  a clean `git archive` export (no .venv/.git) -> head `351b134be5860106`;
  pipeline `autoresearch-watch` binds `https://github.com/karpathy/autoresearch.git`
  (no-op transform). `train.py` in mirror has DEVICE_BATCH_SIZE=32.
- Delta seam verified: `sandman patch --url ...` triggered `autoresearch-watch`
  (job success). The delta itself was rejected on base mismatch (mirror head vs
  local HEAD) — expected; Symphony's bootstrap writes the matching base.

### switched to the operator's fork + bridge code

- Operator: use `theycallmeloki/autoresearch` and consider moving AutoDidact out
  of MiladyOS into it. Ruling: yes to the fork; AutoDidact only into an
  `autodidact/` subtree (own deps), never the minimal core.
- Fork was already at upstream HEAD (`228791f`); remotes now `origin`=fork (SSH),
  `upstream`=karpathy. Mirror binding + `milady_autoresearch` default URL ->
  `https://github.com/theycallmeloki/autoresearch.git` (sandman requires https
  + `.git`; SSH won't bind). Baseline branch `autoresearch/sep9` @ `39769ee`,
  not yet pushed to the fork (awaiting go-ahead).
- Built the bridge: `milady_autoresearch.py` (run_experiment/run_candidate,
  sandman mirror status, Symphony client, record/settle keep-discard),
  8 MCP tools registered in `miladyos_mcp.py`, `AutoresearchEvaluator` in
  `evolve_evaluators.py`, and `autoresearch_evolve.py` (option C driver).
  Symphony became reachable mid-session; control-plane calls verified 200.
- Installed `pi-mcp-adapter` (npm:pi-mcp-adapter v2.32.1) for pi; added a
  `miladyos-local` stdio server (`~/.local/bin/miladyos-mcp` -> MiladyOS
  `.venv/bin/python main.py mcp`) in `~/.config/mcp/mcp.json`. Local Redis
  container `milady-redis` needed because `miladyos_metadata` pings Redis at
  import. Smoke-tested 27 tools over stdio. **Needs a pi restart** to expose
  the `mcp` proxy tool in-session.
