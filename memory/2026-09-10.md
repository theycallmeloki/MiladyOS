# 2026-09-10

## ✅ autoresearch A-path end-to-end + cluster recovery

**A path proven via MCP from the pi chat:**
- `autoresearch_enqueue` (MCP) -> Symphony intent `int-1788991308569846-UDqxjA`,
  state `open`, repo = `https://github.com/theycallmeloki/autoresearch.git`.
- Symphony dispatched it; **RepoDelta bootstrap materialized the sandman mirror**
  into `/data/workspaces/<id>` (`.sandman-src` pinned to `351b134be5860106` +
  fork URL); pi agent ran; parked `awaiting` (~120s).
- Agent transcript carried `RESULT: {"val_bpb": 9.99, ...}`; `extract_result`
  parsed it.
- `autoresearch_settle` (MCP, auto) -> `discard` (9.99 vs best 1.327183) ->
  `close` -> intent `done`; row appended to `results.tsv`.
- Caveat: the shim's llama3.1:8b described tool calls in text instead of
  executing them (workspace stayed clean). Plumbing is correct; a real model
  (qwen3.8-27b) will actually use the tools. Option C also proven earlier:
  `val_bpb 1.328462` -> discard.

**Cluster outage root cause + fix:**
- DHCP handed out dead NTP `68.197.222.112`; Talos nodes gated on time sync, so
  etcd/kubelet never started, apiserver down, all 4 workers NotReady.
- Used `talosctl` (v1.12.5 binary from the 500G backup drive; talosconfig copied
  from `.../home/laneone/talosconfig` to `~/.talos/config`). Patched all 7 nodes
  with strategic merge: `machine.time.servers = [time.cloudflare.com, pool.ntp.org]`.
  All synced, services started, nodes Ready.
- Symphony pod had died with its kubelet; deleted it; Longhorn CSI recovered
  (deleted Unknown manager/csi-plugin pods); image pull stalled on a node without
  the cached image, so pinned the Deployment to `talos-ms4-c7v`
  (`symphony:ee87142780f1` cached there). **Temporary** — revert when pull healthy.
- Ingress is back (401 = up); bridge reaches Symphony at 200.

**Also durable from this session:** `qwen-shim` user service on `:18020` ->
ollama (test harness), ufw `18020/tcp` from LAN + pod CIDR, sandman worker
`miladyos-42` with GPU, autoresearch mirror + `autoresearch-watch` pipeline.

## Real model: qwen3.8-27b on the 3090 (long context)

- Cloned `syv-ai/qwen38-27b-rtx3090` to `~/Documents/qwen38-27b-rtx3090`.
  Docker path is unusable here (overlayfs on btrfs hangs extracting the 9.5 GB
  image), so used the bare-metal venv path.
- `uv venv venv` (py3.14) + vLLM 0.28.0 / torch 2.13.0+cu130; downloaded
  `dbirks/Qwen3.8-27B-W4A16-AutoRound` (19 GB); ran quant_lm_head/embed/mtp,
  build_draft_vocab, fetch_fast_variant; applied all 30 `patches/*.patch`.
  (Careful: my repeated retries ran concurrent requant chains on the same files;
  all completed without corruption, but do not chain these blindly.)
- Installed Arch `cuda` 13.3 for `nvcc` (`/opt/cuda/bin`) — FlashInfer's JIT
  needs it, else every request dies with "Could not find nvcc".
- User unit `qwen-serving.service`: `CTX=long`, `MAX_LEN=150000`, `GPU_UTIL=0.90`,
  `SPEC=mtp`, `PREFIX_CACHE=1`, `CUDA_VISIBLE_DEVICES=0`. Serves `:18020` as
  `qwen3.8-27b`. **KV pool 156,887 tokens (1.05x at 150k)**, fp8 KV.
  `fast` mode would be 64k bf16/FA; `huge` (KVarN 200k) needs `kvarn/install.sh`.
- Mode knobs: `CTX`, `MAX_LEN`, `GPU_UTIL`, `SPEC`, `EXTRA_ARGS` (see README).
  GPU_UTIL must stay <= ~0.90 (desktop uses ~2-3 GB of the 24 GB).

### A-path end-to-end with a real model ✅

Intent `int-1789018801456749-lpvkbg`: enqueue -> Symphony dispatch -> RepoDelta
bootstrap -> **the agent actually executed tools** (ran `ls`, grepped
`DEVICE_BATCH_SIZE`, wrote `done.txt`; workspace `dirty=1`) -> parked `awaiting`
-> transcript `RESULT: {"val_bpb": 9.99}` -> MCP `autoresearch_settle` ->
`discard` -> `close` -> logged. This closes the loop: MiladyOS decides, Symphony
executes, sandman mirrors, the real 27B is the agent's brain.

Cluster note: `talos-ms4-c7v` was cordoned/NotReady during this; removed my
temporary nodeSelector pin so Symphony rescheduled to `talos-ga5-yk4`.

## Organizing + the GPU split for B

- Committed and pushed everything: MiladyOS `main` had 20 unpublished commits
  (now up), autoresearch got the gitignore commit and a new fork branch, then I
  fast-forwarded the **fork's `master`** to our work (`efa8355`, batch 32) and
  **removed the `upstream` remote** — the fork is now foundational, no karpathy
  backlink. The sandman mirror already had batch 32, so no re-mirror was needed.
- **Decided the GPU split** (operator's call, good one): the 3090 stays on
  qwen-serving; the sandman worker is now pinned to **`-gpu 1` = the A4000** so
  sandman jobs can never take the 3090. Verified `sandman nodes` advertises only
  the A4000 for `miladyos-42`.
- **Discovered the sandman execution seam**: workers implement no `handleRun`, so
  `sandman run` silently closes against a worker — GPU work must go through a
  *pipeline* (`--gpu N`) that the control plane places and device-allocates.
  Opened ufw `4343/tcp` for the LAN (the `.15` control plane dials the worker's
  exec port) and proved it: a probe pipeline with `--gpu 1` ran `nvidia-smi`
  successfully on the A4000 host. Deleted the probes afterwards.
- **AutoDidact** read: it is Unsloth + GRPO + vLLM LoRA self-training (QA
  generation -> agentic search -> self-verification), a different domain from
  autoresearch's from-scratch GPT, but the same loop contract. The linkage to
  `program.md` is the contract, not the code: one editable artifact, fixed
  budget, one reproducible metric command, keep/discard on it, never stop.
  Per the earlier ruling AutoDidact should still move to
  `autoresearch/autodidact/` with its own deps (not into the minimal core).

## ISO: dev/production split, dual-console installer, `milady` binary ruling

**Dev entry paths split from production (0308aac).** Every build used to bake
`1350-dev-ssh.chroot` (one operator's SSH pubkey into root) and
`1351-dev-autologin.chroot` (root autologin on serial) — and, because the
installer copies the live rootfs, they landed on installed nodes too. `build.sh`
takes `MILADY_DEV` (default 0); when unset, `build-in-container.sh` deletes both
hooks (`MILADY_DEV=1` keeps them for dev). The installer also strips the serial
autologin drop-in + root authorized_keys from the target, so an install from a
dev ISO is still clean. (PLAN §L2 mentions an "autologin console banner" but
AGENT-FIRST D3 flags blind autologin as a bigger surface — production omits it.)

**The installer was serial-only.** A VGA screendump was 2 colours / 910
non-black pixels: a monitor showed a blank screen, because the installer wrote
to `/dev/console` = the last `console=` (ttyS0). Fix (fa1ee6f, bundled with the
naming docs): `milady-install.service` pins `TTYPath=/dev/tty1`, a new
`milady-install-serial.service` pins `/dev/ttyS0`; both run in the live session
and a `flock` lets only the first to reach the install step proceed. Bootappend
and the installed cmdline gain `console=tty0`; TERM + per-console log follow the
pinned TTY. Verified on 0.0.0.0.704: VGA non-black 0.09% -> 96.75%, serial still
works, full serial-driven install completed.

**`milady` binary ruling (PLAN §Naming; AGENT-FIRST D3/D4).** The old note called
`milady` the "LLM bridge", container-only. It is now the **Go host companion
CLI** (`milady/`: `ask` — replaces the Python mcp-llm-bridge — | `slurp` |
`k3s join` (stub) | `update` | `version`), destined for `/usr/local/bin/milady`
on the host. Four distinct `milady*` objects, none shadowing another: host CLI
(bin) / internal plumbing (`/usr/local/sbin/milady-*`) / OS account (`milady`,
uid 1000) / product (`miladyos`). Ruling recorded. Implementation deferred: the
newest `milady-linux-amd64` release is **0.0.0.0.662** (44 commits behind ISO
.706), so a release fetch would be stale — build from `milady/` in the builder
instead, which needs a Go ≥ 1.27.1 toolchain (trixie's is older; not yet added).

**Clean production ISO:** `0.0.0.0.706`, payload embedded (5.4 GB, reused),
interactive cmdline, dev hooks omitted, dual console.

## ISO boot bugs from a real install test (fixed, eba6a25)

Both reproduced and fixed:

1. **Installed node had no login prompt** ("doesn't boot"). The installer
   removed only `multi-user.target.wants/milady-install.service`, but the new
   `milady-install-serial.service` stayed enabled and its
   `Conflicts=getty@tty1.service serial-getty@ttyS0.service` still applies even
   though `ConditionPathExists=/run/live/medium` later fails — so it stopped the
   gettys. Fix: remove BOTH installer units (symlinks + unit files) from the
   target. Verified on 0.0.0.0.710: login prompt + `milady`/`milady123` login,
   uid 1000, k3s/docker active, ROLE=server, 0 failed units.

2. **Stall/"Timed out" on real hardware.** No virtio MILADY_DOCKER scratch disk
   exists on bare metal, so `/dev/disk/by-label/MILADY_DOCKER` never appears;
   `var-lib.mount` waited the 90s `DefaultDeviceTimeoutSec` before `nofail` let
   boot continue. Fix: `x-systemd.device-timeout=5s`.

**Not a bug:** `systemd-ssh-generator: Failed to query local AF_VSOCK CID:
Cannot assign requested address` — a best-effort generator probe for
SSH-over-vsock with no vsock device; it logs + exits 1 but the boot continues
(role/token/payload follow immediately). Cosmetic; silence with a vsock
modprobe blacklist if it's distracting.

## ISO: Go toolchain + `milady` companion baked in

Per the §Naming ruling, the ISO now ships the host companion CLI:
- `builder/Dockerfile` installs **Go 1.27.1** (pinned to `milady/go.mod`,
  `GOTOOLCHAIN=local` so it can't silently fetch another toolchain).
- `build.sh` bind-mounts `$REPO_DIR/milady` at `/milady` (it lives at the repo
  root, not under `ISO/`) and passes `MILADY_COMMIT`.
- `build-in-container.sh` builds `CGO_ENABLED=0 go build -trimpath` with the
  same ldflags as `milady-release.yml` and stages the result at
  `/usr/local/bin/milady` in the rootfs.
- Gotcha fixed: `$INC` is relative (`config/includes.chroot`), and the build
  `cd`s into the read-only `/milady`, so the `-o` path must be absolute
  (`/build/$INC/...`) — otherwise `go build` fails with "mkdir config:
  read-only file system".
- Verified in the builder: `go version go1.27.1`, `milady version` shows the
  injected version/commit, 11 MB binary.

## AutoDidact Phase 0 — recover + truthful baseline (no training)

**Artifacts recovered** from the 500 GB drive into `AutoDidact/` gitignored
dirs: `saved_data/` (57,733 style pairs, 730 lore Q, 667 grounded, 35
identity, r1 train/eval + grounded variants), `faiss_index/`, `eval/`,
`r1_training/lora` + **`merged-r1c` (3.4 G)**, `lore_training_1b/`.

**Corpus is no longer a lost blob.** `make_lore_corpus.py` regenerates
`data/milady_report.md` (+ `.judge.md` prose variant + `.manifest.json` with
sha256 provenance) from repo docs. Sources now 6 (SOUL, IDENTITY, USER,
MILADY README, MILADYOS README, AGENT-FIRST) after dropping the outdated
SANKO_CHAIN_CONFIG + TRANSFER. ~38.5k tokens, parity with the old blob
verified (only whitespace/current-text diffs, e.g. "Woodpecker" vs "Jenkins").

**Judge validated against the bare-metal 27B** (`:18020`, CTX=long): all four
judge shapes work, incl. `judge_faithful` over 40k chars (38 s).
`judge_correctness` at `max_tokens=256` returns `"\n\nYes"`/`"No"` with an
80-char think — the reasoning block does not eat the content budget.

**The "69/69 anomaly" was a units misreading, not a config bug.**
`eval/history.jsonl` stores `score` as a **count**, not a ratio:
`{"score": 1.0, "n": 69}` means **1/69**. `partial_sanity-comedy.jsonl`
confirms: comprehension 68×0.0 + 1×1.0 = 1/69, adversarial 1/4, comedy 0.096
— in line with every other run. Nothing was misconfigured.

**Trustworthy grounded re-run (same session, same judge):**
| tag | comprehension | comedy |
|---|---|---|
| base-grounded (old) | 17/69 | 0.119 |
| **base-g-v2** (re-run) | **10/69** | 0.114 |
| r1c-grounded (old) | 14/69 | — |
| **r1c-g-v2** (re-run) | **20/69** | 0.107 |
| base-v2 (plain, ungrounded) | 0/69 | 0.097 |

So: r1c 20/69 vs base 10/69 → **p = 0.062 (not significant)**; r1c vs old
base 17/69 → p = 0.70. **The base itself swings 10–17/69 across runs**, so
n=69 is underpowered: neither "GRPO didn't beat base" nor "GRPO helped" is a
supported conclusion. Bigger/less-noisy eval needed before training
decisions. (All categories except comprehension are ~0 for both — the 1.5B
has no identity/style without in-context lore.)

**Milady-voice gap closed in the reward stack.** `r1_rewards.py` had format +
correctness only — a merged LoRA would be a lore-answering Qwen, not
nano-milady. Added `milady_voice_reward` per `NANO_MILADY_DESIGN.md` §Reward
Stack #3: scored on the ANSWER region only, format-gated, two-tier lexicon
caps, anti-corporate-filler penalty, and a density guard (spam 0.32 < milady
0.60, corporate 0.00). Wired into `train_r1.py`; PASS 6 in
`test_r1_rewards.py`.

**`make_nanomilady.sh`** added — the merge step was manual-only. Wraps
`merge_lora.py` with host defaults (era venv + shared HF cache, which already
holds the bf16 base) so a trained LoRA always becomes a servable nanomilady.

**nanomilady context found on the drive**: `~/Documents/nanomilady/` —
`NANO_MILADY_DESIGN.md` (the authoritative plan: reward stack, pitfalls,
milestones), `LORE_SYSTEM_PROMPT.md` + `LORE_FULL_BODY.txt` (the canon
system prompt), and the tracebox/trace-spout/tracefmt/grpo-train sandman
pipeline. Design base is Llama-3.2-1B, but AutoDidact's live path is
DeepSeek-R1-Distill-Qwen-1.5B — that is the nanomilady target.

**Infra fixes required to get here** (would have broken every later phase):
- 27B crash-loop was **VRAM exhaustion**, not a kernel bug: `GPU_UTIL=0.90`
  reserved 21.2 G on a 24 G card with a ~2 G desktop, leaving nothing for
  prefill activations (the `aten::empty` dispatcher errors were the same OOM).
  Now `GPU_UTIL=0.84`, `MAX_LEN=100000` (KV pool 139,705), eager, `Restart=always`.
- `ninja` was missing → FlashInfer/FA JIT aborted every model start.

## Style dataset is unusable as-is (Phase 1 finding, decisive)

The 57,733-pair `milady_style` set was generated by the **7B** style teacher
and screened with the 27B judge. The screen had only run 11 pairs (all
`reject`), which looked like a bug. It is not:

- Random 24-pair sample under the **strict** screen prompt: **24/24 reject**.
- Control pairs prove the judge is correct: verbatim copy → `accept,
  style_present=false`; invented content → `reject`. But it also rejects
  ordinary paraphrase that drops a modifier ("most prominent", "Broadway"),
  because the prompt says to reject missing claims.
- Under a **looser core-meaning bar** (paraphrase + dropped incidental
  modifiers allowed; only invented facts / changed subject / flipped negation
  rejected): **3/16 accept (19%)**.

So the judge is literal-but-right; the **teacher invents facts** ("love
spell", "Charlie", "police probaly lub it", "Goliath and Elisa getting
married"). Re-screening cannot fix this — the style data must be
**regenerated by a grounded teacher (the 27B) and judge-verified**. The
existing 57,733 pairs are not safe SFT targets.

Screening throughput once the server is warm: ~0.7 s/pair at concurrency 4
(16 pairs in 10.7 s) — so a full re-screen of 10,606 candidates is ~30–60
min, and a regenerated set is affordable.
