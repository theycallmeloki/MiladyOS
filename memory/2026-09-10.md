# 2026-09-10

## ✅ autoresearch A-path end-to-end + cluster recovery

**A path proven via MCP from the pi chat:**
- `autoresearch_enqueue` (MCP) -> Symphony intent `int-1788991308569846-UDqxjA`,
  state `open`, repo = `https://github.com/theycallmeloki/autoresearch.git`.
- Symphony dispatched it; **RepoDelta bootstrap materialized the sandman mirror**
  into `/data/workspaces/<id>` (`.sandman-src` pinned to `351b134be5860106` +
  fork URL); pi agent ran; parked `awaiting` (~120s).
- Agent transcript carried `RESULT: {"val_bpb": 9.99, ...}`; `extract_result`
  parsed it.
- `autoresearch_settle` (MCP, auto) -> `discard` (9.99 vs best 1.327183) ->
  `close` -> intent `done`; row appended to `results.tsv`.
- Caveat: the shim's llama3.1:8b described tool calls in text instead of
  executing them (workspace stayed clean). Plumbing is correct; a real model
  (qwen3.8-27b) will actually use the tools. Option C also proven earlier:
  `val_bpb 1.328462` -> discard.

**Cluster outage root cause + fix:**
- DHCP handed out dead NTP `68.197.222.112`; Talos nodes gated on time sync, so
  etcd/kubelet never started, apiserver down, all 4 workers NotReady.
- Used `talosctl` (v1.12.5 binary from the 500G backup drive; talosconfig copied
  from `.../home/laneone/talosconfig` to `~/.talos/config`). Patched all 7 nodes
  with strategic merge: `machine.time.servers = [time.cloudflare.com, pool.ntp.org]`.
  All synced, services started, nodes Ready.
- Symphony pod had died with its kubelet; deleted it; Longhorn CSI recovered
  (deleted Unknown manager/csi-plugin pods); image pull stalled on a node without
  the cached image, so pinned the Deployment to `talos-ms4-c7v`
  (`symphony:ee87142780f1` cached there). **Temporary** — revert when pull healthy.
- Ingress is back (401 = up); bridge reaches Symphony at 200.

**Also durable from this session:** `qwen-shim` user service on `:18020` ->
ollama (test harness), ufw `18020/tcp` from LAN + pod CIDR, sandman worker
`miladyos-42` with GPU, autoresearch mirror + `autoresearch-watch` pipeline.
