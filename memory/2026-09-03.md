## RunJournal build/job fusion (M1) LIVE + dashboard job detail + tracked chips
- Fusion: BuildFusion poller GenServer (supervisor child) journals job_started/job_finished (sandman watch jobs by pipeline <image>-watch + inputCommits head) + build_succeeded (registry tags/list head12, authless); emit-site (agent_runner) journals delta_emitted + build_submitted + BuildFusion.track; RepoDelta.mirror_head/3 public. RunJournal status derives from LAST LIFECYCLE event (fabric events no longer override coarse status). Tests: reconcile pure-core w/ fake fetchers (5), run_journal lifecycle (3), tracked_repos (5). Commits 7ad09d6 + 14b8c53.
- CRASH-LOOP GOTCHA (production): image 1666bc boot failed :badkey :build_events_enabled — Config.Schema.Observability struct BUILT WITHOUT the new keys when the workflow config lacks them (parse is config-keys-driven, NOT schema-default-driven — root cause not fully explained; local repro identical). Deployed config = ConfigMap symphony-workflow (manifest self-hosted-k8s-private deploy/symphony/configmap.yaml, ArgoCD-managed → manual kubectl patch gets SELF-HEAL-REVERTED: edit git, not the live CM). Fixed via git (commit 6ab3c0f: observability.build_events_* keys) → ArgoCD converge → rollout OK. Hardened code: BuildFusion active?/interval/registry + agent_runner registry access via Map.get so missing config keys disable/ default instead of crashing supervisor.
- Dashboard v2 (0bfc5be9f3a6, pipeline 15): per-job live detail panel (toggle_issue_detail; 1s tick refresh of issue_runs events + transcript tail; event badge + summary helpers), "Tracked on sandman" chips (TrackedRepos module: repos w/ -watch pipeline on .15) → queue_tracked_repo reuses queue_repo_intent; H1 stale-copy fix ("repo scanner" line). Verified live: UI renders, chips present, /api/v1/runs journal 15 issues.
- sandman v0.2.45 + bus-wire + registry flip done earlier (memory 09-02). Dashboard/kaniko image cycle: seed per-file commit → watch job → build-bus pipeline 15 → promote → ArgoCD. Config changes MUST go through the k8s git repo, code through the mirror.
## Fusion real-run E2E VERIFIED + workbench harness direction (user-redesigned)
- Full E2E live: intent int-1788376624909747-4ZG23w (repo = FULL binding URL https://github.com/theycallmeloki/symphony.git — chips were passing bare "symphony", which the delta receiver's gitBindings never matches → silent no-build; fixed) → real pi run in pod (40s, session 01a0638d) → delta_emitted/build_submitted head 372719c15b29610b → watch job 411a336c7729 success → pipeline 16 kaniko → tag 372719c15b29 → build_succeeded → promote → deployed (dashboard 401 healthy). job_started was missing (watch job races the 15s poll; first-sighting-terminal branch skipped it) → fixed to journal started+finished on first sighting.
- commit 0cf160b (362 tests): 3 fixes — BuildFusion first-sighting journaling; TrackedRepos git_url from watch pipeline input.git.url (chips now queue full URL); RepoDelta.commit_after_emit (fold delivered edits into new base commit + marker re-point → safe MULTI-EMIT per workspace; foundation for continuations + deploy-now).
- USER REDESIGN: dashboard = WORKBENCH HARNESS (not ops). Model: prompt → pi runs → natural end turn (done.txt) → UI BLOCKS "ask satisfied" + dirty → [Deploy = build+auto-deploy] OR type next prompt → auto-stitches to END OF SAME pi thread (parked pi-acp session; github-automation pattern: one dumb runner, state via accumulated patches/prompts; pi-acp session-map.json proves parkable sessions). Answered: deploy = mid-run emit agent continues; verify = AUTO read-only pass post-deploy (SOLVED/NOT_SOLVED + evidence on card, manual re-verify, per-intent off); layout = FULL workbench redesign (thread list + driver seat + phase strip; ops → collapsible system drawer). Build order + architecture written to plan-symphony-pi-build-fabric.md "Workbench harness design" (2026-09-03). Next: intent "awaiting" state → SessionRegistry parking → deploy API → auto-verify → UI.
## Workbench harness — built + E2E #2 VERIFIED LIVE (image 84f849e8fa20)
- Harness core ea1c816: intent "awaiting" (completed run parks thread; next prompt reopens, explicit close); SessionPark/SessionRegistry (per-thread pi session survives dispatches — logs "Agent session new"/"Agent session resumed"); runs NEVER auto-emit at end; Deployer.deploy emits parked workspace (POST /api/v1/intents/:id/deploy) + close endpoint; BuildFusion tracks per (issue,head) and AUTO-QUEUES a read-only verify intent on build_succeeded (labels verify + verify_for=thread; config observability.auto_verify_enabled default true); AgentRunner records verify.txt verdict as verify_passed/failed/unclear on the thread's journal.
- E2E #2 (thread int-1788379141426271): prompt → real pi run → awaiting → Deploy API → delta head 32a0dbde9506ec57 → watch job_started+job_finished (both! first-sighting fix) → kaniko pipeline 21 → tag → build_succeeded → verification_started → verify intent open → verify pi run → verify_passed SOLVED on thread → next prompt → "Agent session resumed" (SAME session) → awaiting → deploy#3 w/ recovery → close → done.
- BUGS FOUND+FIXED LIVE: (1) SessionRegistry put_in(state.parks,...) macro-trap → KeyError crash loop (registry + whole runtime restarted every ~3s re-dispatching — one_for_all); fixed put_in(state,[:parks,...]). (2) repo_delta emit passed MARKER revision (3ed974b...) instead of delivered workspace HEAD to commit_after_emit → deploy#2 base mismatch → receiver silently rejected (delta-base pipeline failure state; POST /delta always 200s!). Fix bfd8409 passes head. Recovery: re-stamp workspace .sandman-src revision to the receiver-recorded 2ae6db5... + new prompt + deploy#3 → delta committed on fix head 98bc2cbe → FAILURE CLEARED (state running) BUT the recovery delta skipped its job trigger → re-seeded the fix file as a DIRECT commit (27cebdce) → pipeline 22. (3) sandman files.put API: PUT APPENDS unless ?overwrite=1 — ALL incremental per-file seeds silently doubled content (old+new concatenated); clean-repair via overwrite pass; PROPOSED sandman v0.2.46: default PUT=replace, ?append=1 opt-in (chunked uploader keeps PutFile), fetch-URL path api.go:1085 → overwrite. (4) workbench UI journal_event_summary BadBooleanError on verification_started (payload strings in `or`) → fixed a5ee4b9 (render crash on threads w/ verification events).
- Workbench UI live (UI agent, dashboard_live.ex 2180 lines + css 2188): thread rail (verify intents excluded) + driver seat (phase strip Queued→Agent running→Ask satisfied(awaiting amber block)→Deploy&build→Deployed→Verifying→Verdict; controls Deploy/next-prompt Send/Close/Cancel) + verification sub-entries + collapsible system drawer (ops) + compose hero; verified in headless browser: rail + seat markers + no crash.
- Rollout trail this arc: 4332d354 (clean-tree repair) → 73b4b00b4af5 (fix+UI) → 27cebdce6ca0 (base fix) → 84f849e8fa20 (UI bool fix) = CURRENT. Pipelines 17/18 failed (polluted tree), 19-23 green.
## sandman v0.2.46 — PUT semantics fix (proper release channel)
- CHANGE: plain `PUT /api/v1/commits/{id}/files/{path}` now REPLACES content at the path (idempotent REST PUT); append is the explicit `?append=1` opt-in (log-style accumulation); `?overwrite=1` = compat spelling of the same replace. fetch=URL ingestion + split-upload record parts write as replaces. Client: PutFileAppend/PutFileAppendStream added. CLI put/file put: replace by default (cp-like), `--append`/`-a` flag, --overwrite kept for compat, mutually exclusive.
- IMPORTANT audit finding: the EARLIER "nothing depends on same-path append" blast-radius was WRONG at the contract level — conformance file_semantics (TestAppendWithinCommit/TestAccumulateAcrossCommits) DID encode append-as-default incl. cross-commit ancestry accumulation. The flip is still safe: NO production flow relied on plain-PUT accumulation (mirrors/git-delta/seeders all wanted replace; that's why content doubled). Accumulation remains supported via explicit append=1 + split uploads + cron tick files. Tests migrated: TestPutReplacesByDefaultWithinCommit, TestAppendWithinCommitIsExplicit, TestPutReplacesAcrossCommits, TestAppendAccumulatesAcrossCommits; overwrite=1 compat asserted.
- RELEASE CHANNEL (proper flow, not scp): commit+tag v0.2.46 → `gh release create v0.2.46` with assets `sandman-linux-amd64` + `sandman-linux-amd64.sha256` (update.go refuses unsigned installs; asset = sandman-<goos>-<goarch> + matching .sha256, hex digest) → `cd ~/Documents/sandman-pipelines && SUDO_PASS=l ./roll-update.sh`: fleet `sandman update` per node (workers first miladyos-42/02/03/04, daemon .15 last; restart only on update) then k8s phase (build+push sandman:worker-v$VER from deploy/k8s/Dockerfile.worker w/ VERSION arg, sed hook-configmap workerImage, commit+push origin, ArgoCD hard-refresh, rollout restart sandman-worker-hook, wait worker pods converge on new image). DON'T edit roll-update.sh while /bin/sh runs it (incremental-read Syntax error); needs SUDO_PASS=l (.147 only; others NOPASSWD) or ./.sudo-pass.
- RESULT: 4 workers + .15 daemon all v0.2.46 (my interim scp made .15 "up to date" — byte-identical sha d0ba1066... to the release asset); k8s worker pods talos-3hl-zyf/ga5-yk4/ms4-c7v/ugl-4xf on worker-v0.2.46; hook-configmap bumped in commit c9bb3ad (after tag ad2c95f, matching convention). Live repro PASSED: plain PUT x2 -> replace; append=1 x2 -> accumulates; plain PUT after append -> replace; overwrite=1 x2 -> replace; cross-commit plain PUT replace; CLI put x2 replace + put --append accumulates.
- Reminder for future releases: don't hand-roll binaries onto fleet nodes — publish the GH release first, then roll-update.sh does everything (fleet + k8s) and keeps versions/bytes canonical.
## Symphony UI: polish theme + command center — deployed via bus
- 86fba18 (css +602 "harness polish" green-theme layer + Dockerfile pi-runtime stage node:22 @earendil-works/pi-coding-agent + @geohar/pi-acp, $HOME/.pi pre-created, HOME env) -> mirror 8a23d66d785ed534, pipeline 24 -> deployed aa2f234b167d? NO: 8a23d66d785e deployed (84f849e8fa20 -> 8a23d66d785e).
- 3b88002 (command center hero above rail/seat: operator-action load + 5 lanes queued/active/ask-satisfied/verification/interrupts + next-operator-wait/runtime/tokens; driver brief strip: next action from state×phase, run phase, attempts, verifications) -> mirror aa2f234b167db1cd, pipeline 25 -> image aa2f234b167d deployed. Browser-verified: command center renders (lanes: 16 queued etc.), driver brief on thread select (Closed/terminal/1/2).
- css nuance: appended theme layer sits AFTER base media queries -> re-implemented breakpoints 1180/980/720 in the tail (later same-specificity rules beat earlier media rules). Old-palette straggler fixed: .wb-topbar rgba(15,20,28)->rgba(18,22,20). Phone viewport 390: no hscroll, rail static, single col. Pi 0.84.4 in the pod.
- fa6dcf6 (rail Active/Archive buckets + count pill + filter chip layout: state_filters [active,ask satisfied,queued,open,running,archived]; terminal states done/failed/cancelled -> Archive; default filter active; empty-state messaging) -> mirror a635a527f90f7a18, pipeline 26 -> image a635a527f90f deployed; browser-verified: Active default 16 queued + pill, Archive click -> done/cancelled/failed rows.
- sandman v0.2.47 prep commit 23d7e23: gitDelta recovery-trigger fix (clear stale delta-base failure BEFORE commitRevision so the recovery commit triggers — triggerForCommit skips failed pipelines; old order left recovery commits untriggered = "98bc2cbe built never") + 3 more v0.2.46 fallout fixtures migrated (dag TestFlushChainRepeated, paused-input backlog, accumulated-head) + copy-protection test now documents put-replaces. FULL conformance suite GREEN (exit 0, 829s). v0.2.47 release = tag + gh release + roll-update.sh when convenient.
- sandman v0.2.47 RELEASED + ROLLED (proper channel): tag + gh release (asset sha 17b091a2...) + roll-update.sh SUDO_PASS=l -> fleet .147/.15 v0.2.47, k8s worker-v0.2.47 (configmap bump commit 058a4e8), hook restarted, talos pods converged. Conformance green beforehand.
- symphony 4c4865b "driver-seat workspace-edit list": RepoDelta.dirty_paths/1 (name-only git diff --name-only HEAD + ls-files --others, no content reads), Deployer.dirty_files/1 (parked workspace dry-run), GET /api/v1/intents/:id/dirty -> {paths,dirty}; LiveView: brief Edits cell + Workspace-edits block refreshed per tick while awaiting. -> mirror 6977dcf7cf73, pipeline 27, deployed. E2E: intent int-1788386798017537 (append README line) -> run -> awaiting -> /dirty = [README.md, done.txt] -> seat brief EDITS 2 + block rows -> close -> done. 366 tests (2-3 known CoreTest timing flakes only).
- heex edit gotcha: the edit transport mangles PUT content containing '<'+letter (e.g. <div, <span) — XML parser closes SM:PUT early ("no <SM:PUT>"). Workaround: python patch scripts building tags from chr(60), or keep heex out of FIND/PUT. "<." and "<%" and "</" are safe.
- sandman-pipelines 8cb6279: nuked tools/pipelinebuilder (CrewAI Jenkinsfile codegen; E2E never ran — waited on a capable LLM backend; Jenkins deprecated). Superseded by the symphony/pi harness. 761 scaffolded x402 Jenkinsfiles + 17 spec-only templates stay as the payload; per-job main.py quality unproven (weak-LLM dry-run only). Discussion opened: x402 jobs -> sandman-native on-demand (image from miladyosregistry, scale-to-zero by standby), porting driven by harness; open policy: paid-endpoint prove direction (local impl vs fixture probe vs live paid call); candidate pilot: address-geocode (local Nominatim impl, no wallet).
## verify controls SHIPPED (8602a52) + two seat-crash fixes (1bec00d, c12b811)
- Schema-declared observability.auto_verify_enabled (default true) — the global gate was defunct (Map.get on an undeclared field; WORKFLOW.md couldn't set it).
- Per-thread opt-out: labels ["no-verify"] → no auto-verify pass after build (Intent.verify_disabled?/1); gates AUTO only.
- BuildFusion.verify_thread/2 public chokepoint (auto + manual): dedupes one non-terminal pass/thread (verify_pending), journals verification_started once.
- POST /api/v1/intents/:id/verify: resolves last build_succeeded head from the run journal; 409 for unsettled/verify-intents/no-built-head/verify_pending.
- Driver seat: Verification block (Deploy/Verification/Next-prompt siblings) w/ Verify build button + auto-verify-off note.
- ROOT CAUSE of the "driver seat broken" report: TWO undeclared-assign KeyErrors (seat_controls @built_head/@verifications — fixed 1bec00d; driver_seat forwarding built_head without declaring/receiving — fixed c12b811). Symptom: awaiting-thread selection → render crash → silent LiveView reconnect (phx-session persists across reconnects — session probes were useless) → compose hero. Diagnosed via LOCAL escript repro (copied pod binary, --logs-root, REDKA_URL pf to redka.default via 6380; the debugger's own 6379 local squat confused things once) — log file showed the exact KeyError.
- ALSO fixed: Verification block was nested inside the Deploy card (balanced divs, cosmetic).
- E2E (live, image 9c7d61b4000e): no-verify thread built (08a4f502, pipeline 29) → NO auto-verify spawned → POST /verify 200 (int-...MVi1rA) → second POST 409 verify_pending → pass ran → journal tail: verification_started → verify_passed.
- redka FLUSHED on user request (35 intents incl archive dropped; DBSIZE 0); one auto "Repo job: symphony" raced in + was cancelled; clean slate = 1 no-verify thread + verify pass + cancelled repo-job.
- Deploys this session: e05416a2a746 → 08a4f502 → aee224a3f773 → 9c7d61b4000e (pipelines 28-31). Recreate-strategy gap caused 503 windows + dead sockets mid-rollout (user's "UI broken" overlapped this).
- blue-green scoping done (enisoc BlueGreenDeployment CRD + controller live, miladyos precedent; effort = manifest swap + RWO same-node affinity + app leader lease for double-dispatch; user DECLINED for now — x402 + harness items stay the priority).
## x402 pilot (address-geocode) — task 1 DONE + task 2 in flight
- Job repo shape (mirror-native, validated end-to-end): sandbox mirror repo = ENV-SAFE name (git-input deriveGitRepos: input NAME == mapped repo; hyphens illegal → underscore mirror `address_geocode`), payload = Dockerfile/main.py/probe.py/spec.json/job-spec.json/README(.dockerignore/LICENSE). Watch pipeline PROMOTE=0 + REPO_URL basename == mirror. Bus (milady/buildbus woodpecker repo 3 on gitea 172.17.0.1:3000): fetch-context pulls the sandbox repo tree at head → kaniko → miladyosregistry/address-geocode:<head12> (registry image may keep hyphens). Job pipeline registered (probe-gated cmd `python3 /app/probe.py && python3 /app/main.py`, tick @every 24h, default env QUERY) + on-demand /trigger → success. Images: 086b78b873b9, 9f5cbac92d51, 8135d6ce7755 (clean v2), 66e5949a71? (35 pushed nothing).
- bus-wire.py fixed (f4cc96a + 09bf504): top-level secrets rejected by strict decode → transform.secrets only; hyphenated repos → env-safe input name; PROMOTE default now 0 (opt-in). Two earlier promotes (PROMOTE=1 default + hyphen URL) mis-targeted deploy/symphony/deployment.yaml with the address-geocode image (restored 071795b + a3ae0d3; git-managed configmap+secret → ArgoCD self-heal: workflow config edits MUST go through self-hosted-k8s-private deploy/symphony/configmap.yaml).
- INCIDENT: pi runs broke after the address-geocode damage window — SessionRegistry.start_park BadMapError (park_ready {:ok, []}) crash-looped dispatch on a stale pod; cleared by deployment restart. THEN: pi turn_timeout 30m too short for no-local-python port work → configmap pi.turn_timeout_ms 7200000 (b79f34b). THEN: model maxTokens 1024 (qwen3.8-27b, pi-config secret models.json) truncated every pi response ~1KB → long file writes corrupt → secret patched to 8192 + restart (secret NOT git-managed; subPath remount needs pod restart).
- Port intent (LySI7g, take 2) in flight ~2h+: pi wrote a 345-line real Nominatim main.py (node v22 in-pod used to mirror probe assertions locally since no python3); parked/awaiting pending.
- Remaining: park → deploy → rebuild → update job pipeline image → trigger job → probe green → register final + verify on-demand = tasks 2-3.
## x402 pilot COMPLETE (address-geocode, real geocoder) — tasks 1-3 DONE
- Port: harness (pi) rewrote main.py as a REAL OSM Nominatim geocoder (stdlib urllib, UA, live + offline-fallback, probe-green: missing_query/valid_query/limit_bounds/deterministic). Seeded ddba54c9 (image address-geocode:ddba54c93320, bus pipeline 39). Job pipeline address-geocode (probe-gated `python3 /app/probe.py && python3 /app/main.py`, tick @every 24h, env QUERY default) updated + on-demand trigger -> SUCCESS; result.json top hit = White House lat 38.897639 lon -77.036552 source nominatim (real!). "Test passes in sandman" confirmed.
- PLATFORM FINDINGS (footguns for the mass port):
  1. pod had NO python3 -> port tasks can't run probe locally (added python3+curl to the runtime image: 8ff0aeb/b8967f03; now pod = python 3.11.2).
  2. model maxTokens 1024 (qwen3.8-27b pi-config secret models.json) truncated EVERY pi response ~1KB -> long file writes corrupt; bumped 8192 (secret NOT git-managed; subPath remount needs pod restart).
  3. pi turn_timeout 30m -> configmap pi.turn_timeout_ms 7200000 (git-managed: self-hosted-k8s-private deploy/symphony/configmap.yaml; ArgoCD self-heals manual kubectl patches).
  4. RESUMED pi sessions (post-stall-restart) never signal turn completion -> permanent stall/retry loop; first sessions park fine. CANCELLING an intent DELETES its workspace (take-3's 3.5h work lost that way).
  5. Mirror/workspace delta-base: a mirror built from interleaved commits-API + gitDelta stamps drifts marker/base -> harness deploy silently fails the base check (submitted:true but no commit). Deterministic path = seed via commits API. bus-wire stamp should set marker = own head id for first-deploy match.
  6. dashboards: symphony deploy image got mis-targeted TWICE by the address-geocode watch (bus-wire PROMOTE default was 1 -> fixed to 0, 09bf504) — restored 071795b + a3ae0d3.
- Earlier: verify controls E2E (no-verify + manual verify) completed; redka flushed on request; blue-green scoped + declined; pipelinebuilder nuked.
## Delta-base drift + silent deploy drop HARDENED, released v0.2.48, fleet rolled
ROOT CAUSE of the silent pilot deploys: address-geocode-watch's git input keyed the HYPHEN url (bus-wire wrote input.git.url=args.repo_url raw) while every workspace delta carried the UNDERSCORE mirror url -> gitBindings bound ZERO pipelines -> gitDelta 200 ok, NO commit, NO failure; symphony treated 200 as delivered. Fixed 4 layers:
1. bus-wire (fc1c754): input.git.url = mapped_url (the mirror url REPO_URL also carries) - the binding key every emitter uses.
2. sandman gitBindings (515259f): an input ALSO binds when in.Repo == gitRepoName(event url) - spelling drift can't unbind a watch (custom-name maps to a different repo are never swept in).
3. sandman gitDelta/gitDeltaH: response now reports {applied, reason, head}; unbound/base-mismatch = applied:false with reason (still 200; bound pipelines still fail async as before). client.PushGitDeltaReport decodes it.
4. sandman FinishCommit: an authored (commits-API) commit with no explicit .git/HEAD op records its OWN id as the marker (was: inherited stale marker -> two workspaces either side of a seed shared a base and the guard couldn't stop stale overwrites; interleaved-lineage drift). Push/delta commits carry their explicit revision - untouched. View resolved once, shared with the conflict check.
5. symphony RepoDelta (3710681): emit_delta confirms delivery - reads the receiver's applied report, falls back to a mirror-head marker read-back for old daemons; unapplied = {:error, {:delta_not_applied|:delta_unconfirmed}} -> deploy API 502 deploy_failed (was: success with no commit). Pure helpers delivery_outcome/1 + confirm_delivery/5, 19 repo_delta tests green (docker elixir:1.19.5-otp-28, MIX_HOME=/home/laneone/.cache/mix-hex).
TESTS: 3 new conformance (spelling-drift binding; unbound url applied:false; authored seed resets marker + refuses pre-authoring base). Full gates green: go test ./... EXIT=0; conformance 828s ok (bg).
LIVE REGRESSION (pre-roll, v2.47): fixed the watch url (v3) -> new intent 5MbPvQ (README line) -> deploy committed 1b337ca1 (was: silent) -> watch job bfe82779 success. Intent closed.
RELEASE: v0.2.48 tagged + pushed (515259f), workflow published assets, roll-update.sh run (SUDO_PASS='l'; SANDMAN_REPO=/home/laneone/Documents/sandman). Fleet: daemon .15 v0.2.48; workers 147/133/32/241 v0.2.48. k8s: hook-configmap bumped (910e43f) -> sandman:worker-v0.2.48 on all 4 talos pods ready. The v0.2.47 roll from the prior session is superseded (everything now on v0.2.48).
Local toolchains: go at /home/laneone/go-toolchain/go/bin (NOT on PATH); elixir via docker image elixir:1.19.5-otp-28 mounting the repo + MIX_HOME volume (hex/rebar installed there).
## Harness sharp edges 2+3 closed (symphony df5b329, live aa03edf059cd)
Per user: forego per-intent turn budget; close the other two.
- STALL/RESUME LOOP: (a) pi_acp now EMITS tool_call/agent_message activity (was: tool
  phases streamed nothing -> false-positive stall kills of working runs; also invisible
  in transcripts); (b) codex.stall_restart_cap default 2 (0=legacy): a redka-tracked run
  stalled past the cap parks `awaiting` WITH workspace (journal completed w/ error) instead
  of the unbounded backoff-restart churn. Read-only trackers (no notify_run_finished park)
  keep the legacy restart path (gated on tracker.kind == "redka").
- CANCEL-WIPES-WORKSPACE: reconcile terminal branches (running/blocked/retry-lookup/init
  sweep) skip workspace cleanup for state "cancelled"; deployable now includes cancelled;
  deploy finds the retained dir via new Workspace.path_for/2 (registry park was stopped on
  cancel); successful deploy re-parks the thread awaiting via explicit
  IntentStore.recover_cancelled_intent/1 (NOTIFY-run-completion still never resurrects a
  cancel - redka terminal contract test kept green).
- ALSO: repo_delta.ex 3710681 (delta delivery confirm) was committed but NEVER seeded to the
  mirror - this ship included it (the pod lacked the applied-report verification).
- Tests: pi_acp tool-liveness ordering, intent_store cancel->recover->close, workspace
  path_for derivation; full suite 379 tests - only the 2 known CoreTest timing flakes.
- Shipped: seed aa03edf059cd (bus pipeline 42) -> deployment rolled out. LIVE E2E:
  intent fucxmg parked -> cancelled -> dirty STILL listed after 75s of reconcile ticks
  (old code wiped <3min) -> deploy from cancelled OK (mirror 7febca29) -> re-parked
  awaiting -> watch job 3b52cdd1 success -> closed.
- Toolchain notes: mix via docker elixir:1.19.5-otp-28 + MIX_HOME=/home/laneone/.cache/mix-hex;
  elixir edits compile+test in the container. Backticks inside bash single-quoted heredocs
  still get substituted by the bash tool - write JSON payloads via python heredocs.
## Full-pass review (sandman/symphony/github-automation) + plan-symphony-pi-build-fabric CLOSED
User asked: full pass; mark TODOs done; delete the plan file if complete (memory has it). Plan WAS
complete as a plan (milestones M1-M5 + workbench + delta hardening + port template all shipped;
batch = standing ops, not plan work) -> DELETED plan-symphony-pi-build-fabric.md. This memory file
+ commits are the record. Loose ends surviving deletion (session-tracked): (1) ~889-job batch -
user picks repos; template: bus-wire --name <job>-watch --repo-url .../<mirror>.git --image <job>
--buildbus-id 3 (PROMOTE=0) -> seed via commits API -> intent (python3+8192+2h) -> deploy -> update
job pipeline image -> trigger -> probe green. (2) workflow-config parse is config-keys-driven not
schema-default-driven (missing keys drop fields at boot; badkey crash was Map.get-hardened, root
fix = parse from schema defaults). (3) H1 "hub consolidation" ref in the old plan was ambiguous -
stale, dropped.
REVIEW FINDINGS (3 scouts + spot-checks; verified claims):
- sandman LATENT bug: input-tree walkers diverge - deriveGitRepos walks Cross/Union/Join/Group but
  deriveCronRepos + deriveTriggerBranches walk only Cross/Union -> a cron/size-trigger input under
  Join/Group is invisible (no repo/ticker/accumulate). Live check: 6 pipelines, NONE nest under
  join/group -> latent; fix = one shared walker + conformance test when next touched.
- sandman drift class: store PutFile=APPEND vs client PutFile=REPLACE naming inversion; 6 client
  write entrypoints over 2 store primitives; ?overwrite=1 never branched on (compat only); STOP==
  CANCEL alias w/ distinct docs; two docker-run stacks (legacy job.go text-protocol vs
  runner.go containerRunner); .git/HEAD marker semantics live in generic store FinishCommit.
- symphony: orchestrator branches tracker.kind=="redka" (orchestrator.ex:653, my df5b329) -
  contradicts Tracker seam ("orchestrator only depends on read callbacks"); fix = derive park
  capability from adapter notify_run_finished presence, not kind string. Vestigial "running"
  intent state (no writer). Status vocab (completed/blocked/failed/issue_terminal) in 3 files no
  shared enum. RunJournal multi-writer (4 producers, O_APPEND per line, no serialization).
  Dead: park_intent, RepoDelta.best_effort_emit/3, Issue.label_names; RepoDelta.emit_delta takes
  Issue it ignores while Deployer re-maps intent->Issue duplicating redka to_issue. Stale docs:
  AgentRunner "pi default" (runtime default is codex). Dashboard re-derives state machine
  (3rd consumer of state names + UI-only archived/active).
- github-automation: prompt front-matter (description/argument-hint, `make prompts` -> pi install)
  vs symphony WORKFLOW.md = same "prompt as source of truth" intent, two mechanisms (different
  scope: task-type prompts vs repo workflow - benign, worth naming). CONTEXT_ZIP fork/resume
  obsolete vs parked pi-acp sessions. README stale (denies the wine build that exists). wine/
  ccache expertise + wine-*.md prompts = owned-alone domain (boundary, untouched per user).
- Deliberate-clean confirmations: symphony intent transitions single-owner (IntentStore
  transition_intent/4); orchestrator->Tracker read-only seam holds except the kind branch;
  RepoDelta=mechanics vs Deployer=policy split coherent; AgentRuntime normalize step identical
  across impls. sandman store/DAG + client + conformance all exercised (nothing dead).
## Review four-fix pass SHIPPED (symphony b8484bb / sandman 1c04273 + v0.2.49 rolled)
1. SEAM-FIX: orchestrator stall-cap parked on tracker.kind=="redka" (scheduler past the Tracker
   seam). Now Tracker.can_park_runs?/0 = active adapter implements notify_run_finished/3 (redka
   true / memory false; test via settings facade). orchestrator.ex:656 uses the capability.
2. SANDBOX WALKER: input-tree recursion existed 7x and DRIFTED - deriveGitRepos/gitBindings/
   gitSideRepos/cronSideRepos walked cross/union/join/group but deriveCronRepos/
   deriveTriggerBranches/accumulateTriggers walked only cross/union (cron/tick side under
   join/group invisible to derivation while cronSideRepos enumerated it). All 7 now share
   walkInputs (git.go); trigger pos counter lockstep by construction. Guard test
   input_walk_test.go nests cron+trigger+git under a group: FAILS on old walkers, passes now.
3. WRITE ALIAS KILL: client.PutFileOverwrite + PutFileStream overwrite bool both sent
   ?overwrite=1 - a param the server NEVER reads (plain PUT replaced since v0.2.46). Deleted;
   PutFileStream(commitID,path,r); CLI put/file put --overwrite flags + mutual-excl gone;
   conformance migrated to PutFile; api.go documents ?overwrite=1 as accepted-and-ignored
   (pre-v0.2.46 clients keep working). CopyFile overwrite + cron/spout Overwrite = REAL, kept.
4. TRIM: IntentStore.park_intent (no caller), RepoDelta.best_effort_emit/3, Issue.label_names;
   Deployer no longer fabricates intent->Issue (emit_delta no longer takes the Issue it
   ignored - repo comes from the marker; submit_build/journal_event run off the Intent);
   docs: AgentRunner codex-default, SessionRegistry/Park runtime-agnostic wording, RepoDelta
   moduledoc (emit = human-deploy + failure-surfacing, not run-end best-effort).
GATES: symphony full suite 379 tests (2 known CoreTest flakes only); sandman unit+client+cli
green; conformance subset 283s + FULL gate 851s ok (ran bg, green post-roll).
SHIP: sandman tag v0.2.49 (1c04273) + gh release + roll-update.sh SUDO_PASS=l -> fleet 4
workers + .15 daemon v0.2.49 (probe: "sandman v0.2.49 is up to date"); k8s worker-v0.2.49 on
all 4 talos pods (hook-configmap main 910e43f..976f60a, hook restarted). Symphony: seed
cb36b76f450c (bus pipeline 44) -> promote 3260e30 (self-hosted-k8s-private main) -> ArgoCD
app websites-private needed a kubectl patch sync (auto-refresh lagged) -> rollout complete on
cb36b76f450c. LIVE SMOKE of the refactored emit path: intent _jUSCw parked -> deploy ->
mirror bde179c4 committed -> watch 682039681398 success -> closed done.
GOTCHAS: roll-update probe = ssh laneone@host + sudo sandman update (idempotent, prints
"up to date"); ArgoCD app sync lag -> patch application websites-private w/ operation.sync;
intents API create: activate must sit OUTSIDE "intent" in the POST body.
## ability-check — SECOND x402 port E2E (full stack, all green) + bus-wire bug fixed
Job: templates/ability-check.Jenkinsfile (Random tag, d20 check resolver; deterministic +
seedable, no network). Full chain OBSERVED end to end on the harness:
1. Payload: extracted spec.json/Dockerfile/main.py from the template; wrote probe.py
   (6 starter contract tests), job-spec.json (probe-gated cmd python3 /app/probe.py &&
   python3 /app/main.py, env ABILITY_DC=15 MODIFIER=5 ADVANTAGE=true DISADVANTAGE=false
   SEED=2, cron @every 24h), README, .dockerignore -> all locally green vs scaffold.
2. bus-wire BUG (fixed 54ee58f): the env-safe-rename rewrite stripped ".git" and only
   re-appended it on the hyphen path -> wiring a NON-hyphenated repo (ability_check)
   400 "clone URL is missing .git suffix". mapped_url now = parent + env-safe basename +
   .git unconditionally. Also learned: pipeline create REQUIRES the mapped repo to exist
   (404 input repo not found) -> POST /api/v1/repos first; DELETE pipeline also deletes
   the repo unless keepRepo (lost ability_check once, recreated).
3. Watch: ability-check-watch (git.url .../ability_check.git, PROMOTE=0) -> seed d1f4ab84
   (commits API, marker self-records) -> watch job 24b3f93a success -> wp pipeline 46 ->
   image ability-check:d1f4ab848203 (baseline scaffold image).
4. Intent v3k3-w (no-verify): pi audited main.py -> env_int hardening (malformed int =
   clean nonzero exit), unified stdout/file serialization; added 7 probe tests (12 total:
   seed reproducibility per mode, boolean parsing variants, modifier 0/negative, DC 0,
   stdout==file, malformed-int fails clean). Parked ~9 min.
5. Deploy -> mirror 607ebfbe -> watch 622f0800 success -> wp 47 -> image 607ebfbedac7.
6. Job pipeline ability-check registered (image 607ebfbedac7, probe-gated cmd) ->
   POST /api/v1/pipelines/{name}/trigger -> job 74ec17515512 SUCCESS.
7. Output verified: repo ability-check@master result.json real contract output (seed 2,
   advantage [2,3] -> natural 3 + 5 = 8 < DC 15 -> success false). Intent closed done.
GAPS noted: probe.py count confusion was MY copy bug (payload dir still had old probe);
python urllib POSTs get redirect-downgraded to GET on this API (use curl with activate
outside intent); job id prefix = hostname (miladyos-sandmand-<id>); output repo = pipeline
name (hyphen) while mirror repo = underscore; pipeline delete wipes repo (keepRepo!).
wine-reference insight applied: probe-as-contract-gate + deterministic seed (like wine
ccache determinism) = the job's built-in gate; port template now exercised twice.
## sandman-pipelines OPS runbook + extractor (commit 1ad0882) — churn enablement
User goal: local qwen3.8-27b keeps churning x402 jobs end to end with drift
automated away. Added to sandman-pipelines:
- OPS.md: the full churn runbook distilled from the two verified ports
  (ability-check = template of the runbook). Phases A-I each with a HARD
  gate: extract+local-green -> wire (repo create FIRST, bus-wire) -> seed
  (commits API only) -> baseline build (proves bus per repo) -> harness
  intent (curl create, activate outside intent, no-verify label, brief
  makes pi own probe deepening) -> REVIEW dirty before deploy -> deploy
  (verify mirror head == watch inputCommit == job image tag) -> register
  job (strict API pops) -> trigger (/trigger not cron/run) + REAL
  result.json read -> close. Drift-guard invariants table (9 laws, each
  bit once), failure playbook, batch rhythm (sequential, spot-check every
  5th, three-tags-agree invariant), automation surface.
- tools/extract-template.py: payload extraction w/ 3-mode probe
  regeneration (A self-contained verbatim / B result-assert rewrapped to
  run main.py + template asserts in-image / C skeleton). Bugs fixed while
  validating: matched-delimiter triple-quote regex (content docstrings
  contain """ after ''' wrappers -> truncation), f-string brace doubling,
  assertion indentation into main(), stripping template import lines
  (function-local import shadowed sys). Validated vs BOTH verified jobs:
  ability-check mode B spec byte-identical + probe green; address-geocode
  mode A env defaults exact + suite green.
- staging/ gitignored (extractor default outdir).
Next: pick jobs smallest-first; churn one at a time; every deploy check
mirror head12 == watch inputCommit == job image tag.
## Port #3 (accounts-receivable-aging-score) FULL E2E + WRITEBACK VERDICT + tool fixes
User hypothesis tested: "downstream pipelines can't write back to sandman-pipelines."
VERDICT: CONFIRMED by topology + pod evidence. Job/pi workspaces = remote-less git
checkouts at /data/workspaces/<intent> on the symphony pod: no /home/laneone (no fs route
to the host checkout), no git remote, no credentials by design (delta-only delivery;
pod has a GITHUB_TOKEN env but the workspace model never pushes and adding a github
writer would violate the credential-less invariant + double-write the repo). Writeback
is a HOST action by construction -> tools/promote-port.sh does OPS.md phase J in one
command (fetch verified mirror payload -> ports/<job>/ + PORT.md + PORTS.md row + git rm
Jenkinsfile + commit). Verified live on port #3 (commit e5e342a).
Port #3 ran clean on the runbook: extractor env extraction missed digit-bearing param
names (AR_31_60/61_90/90_PLUS: regex [A-Z_]+) -> fixed to [A-Z_][A-Z0-9_]* (587d4ab);
mode A probe suite green locally; wire+seed 106e19ac -> watch 4873aa84 -> wp48 ->
image 106e19acb05c; intent 5L_44g parked ~7.5min (pi kept probe suite, contract checks);
deploy 2234b264 -> watch 9c01f9a1 -> wp49 -> image 2234b2646e12; job pipeline registered
(probe-gated) -> trigger -> job f0be1aff4441 SUCCESS; real output: total_ar 770000,
shares current .6494/severe .0519/mid .2987, score 69.19 grade D + component_scores +
drivers. Closed done. ports/ + PORTS.md updated; templates/ queue down 3.
GOTCHA: persistent shell env had SANDMAN_ADDR=192.168.1.15:4242 (no scheme) - curl
tolerates it, python urlopen doesn't -> promote-port normalizes scheme-less addr.
## Port watcher (auto-promote.py) BUILT + LIVE + PROVEN on port #4 (amortization-schedule)
User: make writeback automatic on the host (downstream pi/jobs can't: remote-less
workspaces, no fs route, no creds by design). tools/auto-promote.py = single promote
core, two modes: once <job> (manual phase J; promote-port.sh is now a thin wrapper)
and watch (daemon polling symphony closed job-port intents). Guards: Jenkinsfile still
present + mirror head12 == job pipeline image tag (three-tags) + PORT.md head not
recorded (idempotent). State tools/.port-watcher-state.json + log
tools/port-watcher.log (gitignored). Running via hub (name port-watcher, persist,
restart on-failure, ready log "watcher started").
PROOF (port #4): amortization-schedule (loan math; extractor mode A probe had
GROOVY-ESCAPED \\ line continuations -> SyntaxError -> extractor de-groovy fix
commit 957abb8). Seed f71bf2d6 -> watch ae03964407ee -> wp50; intent HYmO1A parked
~9min (pi hardened main.py: negative-extra guard, overflow guard, full_payment
arithmetic; no probe change - seed probe had 4 solid tests); deploy 5c3bf6ab ->
watch c4c35faee114 -> wp51 -> image 5c3bf6ab9ace; job registered probe-gated ->
job 55dc41c65ecb SUCCESS; REAL output verified (250000@6.5%/360: payment 1580.17,
mo1 interest 1354.17 = 250000*.065/12, balance 249774, total 568861.22) -> CLOSED ->
WATCHER auto-promoted 26s later (log 10:03:33 "promoted: amortization-schedule ->
ports/@5c3bf6ab9ace"), commit 7a099ed, git push.
BRANCH WRINKLE found: whole session pushed to remote master while the repo default =
main (origin/HEAD) - stale main at 09bf504. Fixed: main fast-forwarded to 7a099ed,
remote master deleted, auto-promote now pushes to origin/HEAD default branch
(d2e43b6). PORTS.md = 4 verified rows (address-geocode, AR-aging, ability-check,
amortization-schedule). templates/ queue down to 886 (minus dice-roll etc spec-only
shapes which extractor can't handle - no embedded main.py; separate future pass).
Watcher ops: hub start/restart name=port-watcher; log tools/port-watcher.log;
POLL_SECONDS env (default 90).
## Spec-only generation SUPPORTED + port #5 (dice-roll) E2E incl 2nd watcher catch
Queue reality: 886 templates -> 759 x402-tagged = 91 EMBEDDED (spec+main+probe;
the 4 ports so far) + 668 SPEC-ONLY (contract-only: spec.json = groovy
DOUBLE-QUOTED JSON string, NO main.py). 127 not x402 (ops/legacy k3s-node-join/
gimmeapis/youtube-dl - out of port flow). Extractor now: grabs spec in both
triple-quote and double-quote-JSON forms (unicode_escape decode), spec-only
mode emits normalized spec.json + Dockerfile + job-spec (env from job params
else {} - port defines interface) + PORT PENDING placeholder main/probe
(fail cleanly exit 1; seed image builds, job untriggered until ported).
Fixes along the way: placeholders' \n must be \\n in source (escape becomes
real newline in value -> broken generated files); de-groovy also for mode B
already; committed (spec-only extract + OPS queue-reality + brief text).
PORT #5 dice-roll (first SPEC-ONLY): seed 60b78f9c placeholders -> baseline
wp52 (placeholder image builds ok); intent Ly5b9g author-from-scratch brief
(parked ~10min; deliverables done.txt+job-spec env {DICE_ROLL 2d6, ROLLS 1,
SEED ''}+main+probe); deploy e26ab2cf -> wp53; registered w/ pi env; job
e6040e3d SUCCESS but OUTPUT REPO EMPTY - pi main only printed stdout, never
wrote $OUT/result.json (its self-made probe asserted stdout only) => PLATFORM
OUTPUT CONTRACT gap (stdout alone not captured). Corrective next-prompt via
assign+activate on SAME parked session (awaiting->open, resumes) parked ~15m:
main now writes $OUT/result.json == stdout (mkdir -p); probe OUT-tempdir case.
Deploy 2f21f5a1 -> wp54 -> re-register image -> trigger -> output head
243ad9fe result.json REAL (2d6 kept [3,4] per-die kept/dropped) -> job
e66ea35f success -> CLOSED -> WATCHER auto-promoted ~60s (commit dd42006,
pushed, ports/dice-roll + PORTS row + Jenkinsfile gone). OPS.md now mandates
the platform output contract in every brief (lesson: probe must include
OUT=tempdir result.json==stdout case). ports/ = 5 verified.
## Fork-network lift pass: 2 real lifts landed on symphony main
Research: openai/symphony upstream quiet (tip 8001b52 08-12; we're synced +32 ahead). 2777 forks mostly empty; 4 substantive = sandsower/rondo (409 commits, fs run-ledger), Pimpmuckl/symphony-plus-plus (57k LOC control plane), bohdanpodvirnyi (GitHub review feedback + Projects v2), openclaw/caclawphony; ideas in issuepilot/Swiftyos/fifony/cc-symphony. Ranked by OUR stack fit (pi-acp/redka intents/mirror-deploy/self-hosted model), pulled two:
1. 90c718e lift(env-isolation): fail-closed pi-acp child env (port from rondo ChildLaunchPolicy, trimmed). Pod's GITHUB_TOKEN no longer reaches the agent: Operational allowlist (PATH/LANG/TERM/HOME/USER...; HOME passes - pi auth = FILES under ~/.pi, NOT env; deliberate divergence from rondo's synthetic home) + forbidden patterns (GITHUB/^GH_/SSH/MCP/AWS/AZURE/TOKEN|SECRET|PASSWORD|CREDENTIAL|API*KEY) + explicit pi.child_env_allow exceptions + forced git fence (GIT_CONFIG_GLOBAL=/dev/null etc). KEY FINDING: OTP Port :env option MERGES over inherited env (child = inherited ∪ overrides; verified empirically) - clean-room requires spawning through `env -i K=V ... exe` (exec preserves port pid). pi.child_env_isolation default true (config toggle). 12 tests incl real spawn w/ sentinel env vars.
2. 390616b lift(rework-plan): NOT_SOLVED verify verdict now writes rework.json (schema rework-plan-v1: summary + categorized/severity items w/ concrete change) beside verify.txt (BuildFusion instructions); new Intents.ReworkPlan (lenient parse + prompt block render); agent_runner verdict recorder stores plan on thread intent (NOT_SOLVED replaces, SOLVED clears, UNCLEAR keeps); first turn of next dispatch prepends "## Rework plan" + marks dispatched (stale plans never repeat; new failure re-arms). IntentStore.record_rework_plan/mark_rework_plan_dispatched; intent API map surfaces rework_plan. Idea source: issuepilot ReviewReworkPlan contract (operator-gated categorized rework driving re-dispatch) - the manual corrective-brief step the dice-roll port showed. Suite 397 tests, 2 known CoreTest flakes only.
Both on origin/main. NOT YET DEPLOYED - next: wp pipeline build -> self-hosted-k8s-private promote (symphony image) -> ArgoCD (websites-private) sync; after roll, watch the pi-acp env isolation live (verify auth still file-based) + a NOT_SOLVED verify produces a rework plan on a real port.
Other fork finds recorded for later: rondo RunLedger/event-feed replay (consolidates run_journal), caclawphony gate-transition notifier (103 LOC, when operators parallelize), splusplus token accounting (self-hosted = low value), OpenSymphony MCP workspace bootstrap (careful: agent-side writes = drift risk). NOT for us: review_feedback/Projects v2 (github-tracker), claim leases/MCP-session-recovery (we have park/resume), fifony TS, simphony onboarding.
## Symphony lifts DEPLOYED (26c77b148e91)
User asked "doesn't symphony-watch auto-deploy on repo push?" -> NO: GitHub push alone never reaches the sandman mirror. Evidence: symphony-watch (running on .15) fires ONLY on sandman `symphony` mirror commits; mirror is written by pi deploy deltas or host seeding (never GitHub webhooks). The mirror was at cb36b76f (b8484bb-era + cruft done.txt/docs/e2e-markers.md; README drifted from both b8484bb+HEAD). Deploy ran the full chain:
1. Mirror sync = HOST delta (POST /api/v1/git/delta, payload: url=https://github.com/theycallmeloki/symphony.git, branch=master, base=cb36b76f450c0baa, revision=390616b-sha, files=12 changed-to-HEAD incl README.md, deleted=[done.txt, docs/e2e-markers.md]) -> applied: mirror head 26c77b148e915d04. (Full-tree delta = 117MB JSON, too big - repo carries .github/media/symphony-demo.mp4 30MB; incremental = 126KB. Byte-parity sampled 18 files mirror-vs-b8484bb: only README drifted.)
2. watch job 02079e76 "success" LIES early (wp still running); real build = buildbus wp repo (id 3, "MANUAL PIPELINE @ main" 60900c9c) pipeline #55, kaniko ~5 min -> image symphony:26c77b148e91 in registry.
3. Promote = AUTOMATIC: "sandman build-bus" automator committed deploy/symphony/deployment.yaml itself (438d374, ~1 min after wp55) - the bus automator watches watch-successes and promotes (was disabled/guarded earlier after mis-targets; apparently re-enabled/working now). My manual yaml bump was redundant -> reset --hard origin/main.
4. ArgoCD websites-private sync lagged again -> kubectl -n argocd patch application websites-private operation.sync (revision HEAD) -> rollout complete on 26c77b148e91, pod symphony-67d46d8497-wlv88 Running, /api/v1/state + intents (16) healthy.
ANSWER TO USER: pi-agent commits vs our push are equivalent ONLY as mirror commits; the watch+automator will auto-build+auto-promote any commit that lands on the sandman mirror (pi deltas OR host sync). GitHub repo pushes need the host mirror-sync step (delta or reseed) to enter that loop. Env isolation + rework-plan now LIVE; next real port pass exercises pi-acp spawn under the scoped env (auth still file-based under ~/.pi).
## Improvement pass (user: "what would you change") — 3 shipped
1. VERDICT VISIBILITY (symphony 0f0ae75, LIVE): verify verdict now folded onto the thread's intent.result["verification"] (verdict/verify_intent/at/400-char evidence cap/rework_item_count) via IntentStore.set_verification_result (merge-over-run-result, terminal guard). 3 store tests. Deployed same chain: mirror-sync -> 827ce6acf9a3 (wp56) -> automator c3c92b9 -> argocd nudge (twice: first sync hit pre-promote 438d374, OutOfSync; re-nudge to HEAD) -> pod symphony-c85b84b74-b62gv on 827ce6acf9a3, /api/v1/state ok. (Full suite: 400 + 2 known flakes; the 3rd bg failure was transient same-class.)
2. MIRROR TOOLING (sandman-pipelines 465c031): tools/mirror-sync.py = sha256 parity (git cat-file --batch vs mirror files listing hashes) + one-commit sync via commits API (raw PUT binary-safe, DELETE mirror-only w/ --prune-extra, .git/HEAD kept), --dry-run. Dogfooded: caught 3 drift files (pi_acp_test/repo_delta_test/workspace_and_config_test) beyond intended commit. OPS.md "Deploying orchestration changes" 5-step runbook.
3. DEPLOY-CORRECTNESS: investigation verdict — sandman watch job "success" at t1 while wp55 ran = DISPATCH-ONLY semantics (symphony-watch transform = python one-liner POSTing wp buildbus + exit; sandman job state covers dispatch, not build). NOT a sandman state bug; invariant = registry-tag check (now in OPS runbook). AUTOMATOR GUARD (gitea milady/buildbus .woodpecker.yml, commit 48daad13): promote step now (a) refuses non-symphony sources without explicit DEPLOY_REPO/DEPLOY_FILE (kills the historical mis-promote class that rewrote symphony yaml w/ port images), (b) registry-exists check before committing the manifest (no ImagePullBackOff lines), (c) commit msg carries source repo. gitea = 192.168.1.147:3000, creds milady:milady (admin). Buildbus pipeline shape: fetch-context (tree pull + registry-skip) -> kaniko-build (KanikoBuild CR, waits) -> promote (PROMOTE=1 gate).
NOT implemented (user declined this round, on record): AgentRuntime-boundary env policy for the codex backend + allowlist-can't-re-admit-GITHUB tightening; GITHUB_TOKEN-in-pod audit. Buildbus config lives OUTSIDE the three repos (gitea) — changes need gitea API.
