
## Woodpecker Phase A — Dockerfile rewrite done (feat/woodpecker-migration @ 62224a6)
- FROM jenkins/jenkins:lts-jdk21 -> debian:13.4; plugins.txt/casc.yaml/theme/JAVA_OPTS gone; woodpecker-cli 3.18.0 via woodpecker/install-cli.sh; runtime user milady (uid 1000); tini PID1; startup.sh = woodpecker readiness + tail -f keeper (cli-exec mode, no daemon). Rollback image tagged miladyos:jenkins-last.
- 3 PRE-EXISTING bugs found by boot smoke (old jenkins image had them too — startup.sh MCP/redka NEVER auto-started):
  1. `python` resolves to /opt/hermes/.venv/bin (hermes venv shadows app venv) -> MCP died on colorlog. Fixed: /app/.venv/bin/python explicit.
  2. /data missing; non-root user can't mkdir at / -> redka died -> MCP redis dep blocked. Fixed: image pre-creates /data (milady-owned).
  3. /app/templates + /app/metadata root-owned -> PermissionError. Fixed: pre-created, milady-owned.
- Verified on fresh image: redka up, MCP 6000 SSE 200, oracle/templeos up, woodpecker-cli 3.18.0 ready.
- Next: MCP re-points (create_jenkins_job/execute_command -> woodpecker-cli exec), then cutover cleanup (delete plugins.txt, casc.yaml, jenkins-theme/, templates/*.Jenkinsfile), deploy PV/ingress swap.

## CUTOVER: miladyos now runs miladyos:woodpecker-pilot (host net, privileged, docker.sock + --group-add 981)
- Old container stopped + preserved as `miladyos-jenkins` (rollback: docker start miladyos-jenkins).
- Verified live: caddy (setcap net_bind_service fixed :443), filebrowser (db dirs chowned; auth seeded pre-start — v2.63 min 12-char password, so filebrowser = milady/miladymilady), GoTTY :8088 = milady/milady exact, OS user milady pw milady, MCP :6000 SSE 200, docs :8081, hermes :9119, redka, oracle+templeos, ollama, woodpecker-cli 3.18.0, docker.sock from inside works, cli-exec pipeline ran (docker backend on host daemon).
- More pre-existing bugs found+fixed: filebrowser dirs root-owned (instances died), caddy <1024 bind (always failed), filebrowser users-update-while-server-locked (never applied).

## Phase B DONE: Forgejo + woodpecker-server/agent live in miladyos (branch feat/woodpecker-migration)
- Forgejo 16.0.3 (static binary, sha256-pinned via install-forgejo.sh) :3000 — local forge, SQLite, admin milady, registration disabled, MIN_PASSWORD_LEN=6.
- woodpecker-server from OFFICIAL image (woodpeckerci/woodpecker-server:v3.18.0 via multi-stage COPY) — release binaries are cgo-less, NO sqlite driver; driver name is 'sqlite3' not 'sqlite'. Agent from release tarball (install-agent.sh). CLI restored (a dedup edit had eaten install-cli.sh).
- startup.sh: forgejo bootstrap (app.ini heredoc, admin create, OAuth app via API, secrets -> /var/lib/woodpecker/.secrets incl. GRPC secret), server (:8000 UI / :9000 gRPC), agent (healthcheck :3001 — :3000 conflicts with forgejo). WOODPECKER_OPEN=true (first forge login registers), WOODPECKER_ADMIN=milady (v3 has NO first-user-admin magic — promotes on login). FORGE_PUBLIC_URL=http://192.168.1.147:3000 default — LAN IP REQUIRED so agent step containers (docker bridge) can clone; localhost fails (clone exit 128).
- Verified E2E: OAuth login (browser + curl dance), /api/user admin:true, API token (needs X-CSRF-TOKEN from web-config.js), repo+file via forge API, activate via POST /api/repos?forge_remote_id=N, trigger POST /api/repos/{id}/pipelines {"branch":"main"}, pipeline success on agent 1 (clone+hello, alpine via host docker daemon).
- SHELL GOTCHA (repeat): a comment line inside a backslash-continued env list orphans the PRECEDING assignment (becomes unexported shell var). Happened 3x this session (WOODPECKER_HOST, then FORGEJO* vars). Comments must sit ABOVE the chain.
- EDIT-TOOL GOTCHA (repeat): block-form edits where the rewrite doesn't restate every MATCH line CONSUME the missing lines (ate if-line, heredoc, cli install). Re-read + bash -n after every edit.
- Known: /var/lib (secrets + forge/woodpecker DBs) is container-layer — wiped on reroll, self-healing via startup; PV swap still pending (Phase B follow-up). ni-c MCP + WOODPECKER_CUSTOM_CSS_FILE branding + ci.ingress still Phase B follow-ups.
- Headscale now FTLs on boot (dns.nameservers.global config drift, 0.26.1) — pre-existing, NOT fixed (out of scope this session).

## Forge IP portability fix (commit 1db0c11)
- Dropped hardcoded http://192.168.1.147:3000: forge ROOT_URL + WOODPECKER_FORGEJO_URL now default to the docker0 bridge gateway (172.17.0.1), auto-detected at startup (`ip -4 addr show docker0`), fallback 172.17.0.1. Same value on every default docker host — portability without per-host config. FORGE_PUBLIC_URL env still overrides for LAN-exposed setups.
- CORS was NOT the blocker (OAuth + forge API are server-side/top-level navigations; the SPA never XHRs the forge). The real constraint: clone URLs derive from forge ROOT_URL and step containers sit on the docker bridge, so the forge must be reachable from the bridge AND the host browser. Gateway IP satisfies both.
- Re-verified E2E on the rerolled container: bridge container wget 172.17.0.1:3000 OK, host curl 200, browser OAuth login (button "172.17.0.1"), admin:true, token, repo+file, manual pipeline success (clone+hello exit 0).

## Branding DONE (commits 4b1058e, 2e010bc) — user confirmed working visually
- woodpecker/branding.py generates custom.css + custom.js (logo.svg embedded as data URI, ~455KB each) at build → /etc/woodpecker/, served via WOODPECKER_CUSTOM_CSS_FILE/JS_FILE at /assets/custom.*.
- Header logo: hide a[href="/"] svg, show ::before background-image; dark theme via html[data-theme="dark"] invert. Favicon swap in custom.js (MutationObserver on head).
- Avatar: milady-avatar.png (256px PNG, rasterized via headless-browser canvas/screenshot from logo.svg, PIL webp→png) — uploaded to forge at startup via session-based POST /user/settings/avatar (basic auth 303s; needs cookie jar dance: GET /user/login → POST login → multipart source=local + avatar=@file). Forgejo avatar_url flips to custom hash.
- EDIT-TOOL GOTCHA #4: the branding Dockerfile edit consumed the mkdir/chown RUN (runtime dirs!) — same anchor-consumption; re-verified the layer list after every edit now.

## VERSION PIN INVENTORY — "bump deliberately" reference (future-proofing)
- Pinned (bump = edit pin -> rebuild -> verify -> push):
  - woodpecker-cli/agent 3.18.0: sha256 in woodpecker/install-cli.sh + install-agent.sh (WP_VERSION/WP_*_SHA256 env-overridable)
  - woodpecker-server v3.18.0: multi-stage COPY --from=woodpeckerci/woodpecker-server:v3.18.0 in Dockerfile (image tag; release tarball binaries are cgo-less = NO sqlite driver, driver name 'sqlite3')
  - forgejo 16.0.3: sha256 in woodpecker/install-forgejo.sh (FORGEJO_VERSION/SHA256 overridable)
  - base debian:13.4 (point-release tag); sqlite 3.53.4 (sha256 in sqlite_build stage); milady bridge 0.0.16, caddy 2.4.6, headscale 0.26.1, go1.22.1, sandman 0.2.42, pachctl 1.12.5 (Dockerfile ENV); python deps via uv.lock; image itself via 5-octet MILADYOS_VERSION
- FLOATS (intentional): apt packages (point-release drift, stable ABI), pipeline step images (per-pipeline choice)
- If upstream breaks us: bump the pin through the normal build path — never workaround. Upstream-churn risk is LOW: the CI stack is the most-pinned layer.

## Model B MCP pipeline tooling DONE (commit 46e02e0, branch feat/woodpecker-migration)
- milady's surface (via bridge): read_file/write_file/edit_file (local /app files) + run_pipeline/pipeline_status/pipeline_logs/list_pipelines + repointed create_jenkins_job (creates forge repo + .woodpecker.yml + activates) + execute_command (ad-hoc pipeline in milady/ad-hoc, blocks+streams console like old Jenkins). 16 tools total. E2E PASS on final image (write->edit->run->status->logs->execute_command).
- woodpecker_client.py (new): WoodpeckerClient wraps forge repo/file CRUD + activate/trigger/status/logs. Token read lazily from /var/lib/woodpecker/.secrets (WOODPECKER_TOKEN — startup.sh dance persists it: forge login -> OAuth authorize incl. first-boot grant form POST /login/oauth/grant granted=true -> session -> CSRF from web-config.js -> POST /api/user/token, raw JWT response).
- API QUIRKS (verified live, cost 3 debug cycles):
  - forge contents API: create=POST, update=PUT with sha (POST on existing = 422).
  - woodpecker API {pipeline} path param = pipeline NUMBER, not DB id (ids diverge once >1 repo).
  - v3.18 logs endpoint: one JSON object per line ({id,step_id,line,data}), NOT raw base64 — decode entry['data'].
  - trigger variables DO reach step env ($VAR works); ${VAR} is EATEN by woodpecker config interpolation (empty) — regression #7079 family. Use $VAR unbraced.
  - YAML gotcha: unquoted scalar with ': ' inside splits into a map (yaml.v3) — quote whole scalars.
- FILE TOOL GOTCHA: miladyos_mcp _execute_tool shadows bare Path via a function-local 'from pathlib import Path' in list_evolved_templates — new branches use FSPath alias.
- EDIT-TOOL GOTCHA #5: identical ⟪old│new⟫ selections across branches cross-wired (same-pattern ops); unique-anchor ops + §* sweeps only.
- MCP server restart recipe (pkill self-match trap): script file with pkill "[m]ain mcp" (bracket trick) + docker exec -d to start; su milady needs password (server runs as root via startup.sh).
- Deferred: evolve_* + main.py JenkinsUtils re-point (tier-2), cutover cleanup (remove CLI_EXPERIMENTER_JENKINSFILE etc.), kubeconfig secret tool (set_pipeline_secret), youtube-dl port E2E.

## Evolve re-point + full jenkins-word arc DONE (commits 33ce233, a119f63, abab7cd; pushed; final image rerolled + E2E green)
- Re-point: AlphaEvolve runs candidates on the local agent. Templates -> templates/*.yml (5 groovy ports, # EVOLVE-BLOCK markers, shell gates for param flags, docker:cli+socket for docker). PipelineEvaluator(runner): YAML syntax/static/dry-run + live execution via WoodpeckerClient.run_content into milady/evolve. Evolve evaluators de-groovy'd (DryRunEvaluator/ExecutionEvaluator/CascadeEvaluator(runner)). JenkinsUtils/JenkinsApiError/CLI_EXPERIMENTER deleted; main.py deploy/run/list/view -> WoodpeckerClient; metadata manager params -> pipeline_name/repo_name/pipeline_id; meta_evolve .yml + runner.
- ARC: zero "jenkins" words in tracked files (excl. docs/evolution generated data + memory/). create_jenkins_job -> create_pipeline (repo_name/pipeline_content); JENKINS_ADMIN_ID/PASSWORD -> MILADY_ADMIN_ID/PASSWORD; deleted jenkins-theme/ plugins.txt casc.yaml; python-jenkins dep dropped; ingress -> ci.transparentlyrotatableproxy.site; /var/jenkins_home -> /var/lib/woodpecker; docs rewritten; WOODPECKER_MIGRATION.md -> completed record. Reroll cmd now: -e MILADY_ADMIN_ID=milady -e MILADY_ADMIN_PASSWORD=milady.
- LESSONS: (1) engine runner must be threaded at EVERY construction site (CLI evolve() missed it — live execution silently gated off); (2) duration fitness weight needs normalization (raw seconds × -1.0 swamps scores) -> duration_score 0-1 vs timeout; (3) v3.18 logs/workflows can be JSON null — guard with `or []`; (4) GH release download stall in docker build (milady bridge) — build gets "context canceled" on timeout; retry with cache resumes; run long builds via bash async:true (nohup & dies with the tool shell).

## ni-c MCP wiring — evaluated then DROPPED; bridge made workable; MERGED TO MAIN (a07cc4f)
- ni-c/woodpecker-ci-mcp researched (librarian): TS, npm @ni-c/woodpecker-ci-mcp@0.1.0, node>=22, env WOODPECKER_URL/TOKEN, stdio-only, 71 tools (34 read + 37 write), WOODPECKER_ALLOW_TOOLS=essential -> 8 (list_repositories, get_repository, list_pipelines, get_pipeline, get_step_logs, trigger_pipeline, restart_pipeline, cancel_pipeline), GHCR image exists. rtuszik/woodpecker-mcp = python fastmcp, ~29 read + 5 write, HTTP transport.
- milady-llm-bridge repo researched (librarian): env-only config (MCP_SERVER_URL single SSE, LLM_BASE_URL default ollama /v1, LLM_MODEL llama3.2), tool list derived entirely from that one server, no config file, single-shot CLI with automatic tool loop, v0.0.16 = --version commit only.
- Built multi-server aggregation INTO the bridge (ours): MCP_SERVERS env JSON [{name,type:sse|stdio,url|command,args,env}], MCPClientManager merges tools with collision policy (first keeps bare name, later get {server}_ prefix), routes by exposed name; MILADY_ENV_FILE dotenv; update_template re-pointed to write_file->templates/{name}.yml (old edit_template/create_template are gone from the server); mcp<2 pin (client uses Tool.inputSchema/ClientSession._handle_message — both broke in mcp 2.x); milady console script; 0.1.1. Committed c6ef529 + pushed to github (theycallmeloki/milady-llm-bridge).
- VERIFIED live: manager test = 87 tools aggregated (only collision: woodpecker_list_pipelines), hello_world + list_repositories routed; binary spins with llama3.2:3b then qwen2.5:3b (llama3.2 hallucinated a woodpecker CLI with 87 tools in context; qwen2.5:3b + WOODPECKER_ALLOW_TOOLS=essential is the reliable combo); full chain: run_pipeline->pipeline_status->pipeline_logs, real job executed (clone + echo ad-hoc-run-ok, EXIT CODE 0).
- USER DECISION: drop ni-c entirely ("just getting secrets basically", overkill; our 16 tools cover the CI loop). Nothing in MiladyOS was changed for ni-c (no node/npm in image). Bridge multi-server stays as inert capability (single-server MCP_SERVER_URL default).
- GOTCHAS hit: (1) asyncio.wait_for on client.__aexit__ runs it in a DIFFERENT task than the asyncgen entry -> anyio "cancel scope in different task"; close must be awaited in-task, no wait_for. (2) CancelledError is BaseException not Exception (3.8+) — except Exception misses it. (3) SSE shutdown noise = asyncio logger + unraisable; os._exit(0) + mute asyncio logger + silent unraisablehook in cli_entry_point. (4) dataclass: non-default field after default = TypeError. (5) docker build: `command -v milady` in RUN fails because ENV PATH comes after -> drop the check. (6) git archive HEAD ignores uncommitted changes — the first "3.5s build" was a cache no-op.
- Dockerfile: bridge installed from source git+https@c6ef529 into /opt/milady/.venv (mcp<2, own venv), ENV PATH prepended, MCP_SERVER_URL default. NO ollama bootstrap (added then REMOVED on user call: "unrelated to our jenkins stuff, do it properly on main" — amended commit 5966238->a07cc4f, force-pushed).
- REROLL + IN-CONTAINER SPIN (fresh woodpecker state — ad-hoc repo did NOT exist): qwen2.5:3b called create_pipeline(repo ad-hoc, yaml) -> run_pipeline -> pipeline_status -> pipeline_logs; job SUCCESS (clone+hello, bridge-job-ok, exit 0); full console via pipeline_logs MCP call.
- MERGED: main 80b421b -> a07cc4f (ff), pushed. memory/2026-09-01.md stash dance for checkout (file is tracked now — committed in the arc).

## Cleanup: WOODPECKER_MIGRATION.md deleted (was completed record, merged arc)
- woodpecker/README.md rewritten to current state (was stale: "forge = none / ni-c later / Next (Phase A)" — all obsolete).
- Load-bearing facts preserved (the rest was historical record): (1) woodpecker-ci#2651 wontfix — the Woodpecker server CANNOT run without a forge, hence the self-hosted Forgejo; (2) developers.woodpecker.co/docs/mcp is Woodpecker.co, a COLD-EMAIL SAAS, not Woodpecker CI — ignore it; Woodpecker CI has no official MCP server.

## Commit-message surgery (2026-09-02) — Aug-1 window canonicalized
- filter-branch --msg-filter on `a9ba30b^..main` (87 commits, 2026-08-01 -> now): every subject -> "looks good to milady", body = original message (lossless). Pre-Aug-1 history untouched (bb03733 SHA unchanged); trees verified identical (tree-hash sequence == old origin/main).
- Old history recoverable: `backup/pre-surgery` ref + git reflog; remote was force-pushed (2a7c409 -> e05c595).
- Window had zero LGTM/lgtm/signed variants (all pre-Aug-1); no merges in window.
- filter-branch needs clean tree -> stash memory file around the run.
