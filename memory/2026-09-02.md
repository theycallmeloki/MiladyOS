
## Build fabric live (M1-M3 of symphony control-plane plan)
- M1 kaniko big contexts: hook.py contextConfigMap (mount, no argv) + CRD fields + RBAC configmaps; kaniko-submit.py helper (registry guard, .dockerignore tar, inline-vs-configmap auto, poll, digest). E2E: symphony image rebuilt via kaniko configmap context (was argv-capped). Commit f41f270 (MiladyOS). NOTE: ArgoCD app `kaniko` selfHeals from git — commit+push BEFORE kubectl apply, else reverted.
- M2 agent CI/CD: woodpecker/kaniko-build.yml (manual event, single step, kubeconfig secret via **v3.18 from_secret** (legacy `secrets:` list dead), env map not list, no ${VAR} in commands, kubeconfig written to CI_WORKSPACE (steps are separate containers)). Tool image kaniko-submit:1 (python3.12-alpine+kubectl 1.30.2). E2E: kaniko-hook:3 built via WP→kaniko. Commit 93c2391.
- M3 sandman bus: repo builds + pipeline kaniko-builder (spec JSON -f; CLI --secret bug EnvVar w/o Key → 400; transform NEEDS cmd else passthrough-noop; datum env = input repo name $builds NOT $in; subtree datums nest $builds/<datum>/spec.json). Secret kubeconfig (key KUBECONFIG_CONTENT). Images kaniko-builder:3. E2E: sandman put builds@master:kaniko-bus-demo → kaniko-bus-demo:1 in registry. sandman-pipelines commit ec167a0 (templates/kaniko-builder/ + README gotchas). Daemon .15:4242 (v0.2.43), CLI on .147, cluster API 192.168.1.45:6443 LAN.
- NEXT M4: AgentRuntime behaviour (agent_runner AppServer calls → codex + PiAcp impls), pi-acp adapter (spawn pi-acp, ACP client: session/new+prompt embeddedContext, agent_message_chunk→on_message), runtime image node+pi+@geohar/pi-acp, qwen3.8-27b openai-compatible (vLLM .147) config via secret. M5: redka intents.

## M4 pi runtime (in progress)
- AgentRuntime behaviour (lib/symphony_elixir/agent_runtime.ex): start_session/run_turn/stop_session; impl selected by agent.runtime config (:codex default | :pi_acp). Codex impl = pass-through AppServer (unchanged). agent_runner.ex now goes through the seam.
- PiAcp adapter: spawns `pi-acp` (ACP NDJSON over stdio; pi-acp spawns `pi --mode rpc`). Protocol (observed pi-acp 0.3.1/pi 0.84.4): session/new needs mcpServers:[] + sessionCapabilities; output = session/update notifications (agent_message_chunk w/ content.text); turn ends at session/prompt RESPONSE {stopReason}. Text chunks buffered, flushed as :message on turn end (1 journal line/turn). Startup banner dropped (multiline regex ^pi v\d+\.\d+[\s\S]*---). 2 hermetic tests (stub pi-acp script) green; full suite 303 (same 2 pre-existing flakes).
- REAL smoke passed: adapter drove real pi-acp→pi (deepseek on this box, ~/.pi mounted, HOME=/home/laneone) → "SMOKE-OK" :message event + turn_completed end_turn. Smoke gotcha: mix run boots dashboard TUI — append System.halt(0); container needs -v nvm + -v ~/.pi, HOME=/home/laneone (MIX_HOME/HEX_HOME stay /tmp/sym-home).
- pi provider reality on .147: default deepseek (auth.json), models.json vllm=Gemini mask. qwen3.8-27b vLLM LIVE at localhost:18020 (/v1 OpenAI, model qwen3.8-27b) — add pi provider for it in runtime config. NEXT: runtime image (erlang base + node22 + pi + pi-acp + .pi config via secret, defaultProvider vllm-local qwen), orchestrator-level smoke (WORKFLOW.md agent.runtime pi_acp + memory tracker issue → journal transcript), commit.

## M4 DONE (pi runtime live in cluster)
- Full-loop smoke PASSED: WORKFLOW.md (---frontmatter--- format! kind memory) agent.runtime pi_acp + memory-tracker issue (app env :memory_tracker_issues) → orchestrator dispatch → real pi (deepseek) wrote done.txt + replied PI-DONE → journal run completed w/ transcript (session_started/message/turn_completed) + session_id. Gotchas: updates MUST carry timestamp (orchestrator handle_info matches %{event:,timestamp:} else drops); WORKFLOW.md = YAML frontmatter between --- markers; smoke paths under /tmp/sym-home (root-owned /tmp dirs block host cleanup; clean inside container).
- Runtime image 0.0.3-pilot-pi: multi-stage node:22-bookworm-slim (npm i -g @earendil-works/pi-coding-agent @geohar/pi-acp) → COPY /usr/local/bin + node_modules into hexpm erlang final; pi 0.84.4 verified in pod. Pi config = secret symphony-pi-config (settings.json defaultProvider vllm-local/qwen3.8-27b + models.json baseUrl http://192.168.1.147:18020/v1 + auth.json deepseek fallback + extensions/pi-acp.json quietStartup) mounted via subPath at /home/symphony/.pi (readOnly); HOME=/home/symphony env. qwen3.8-27b vLLM on .147:18020 confirmed live.
- GOTCHA: ArgoCD app **websites-private** (NOT obvious — check kubectl get application -n argocd full list) manages default/symphony deployment w/ selfHeal → kubectl apply reverts; COMMIT+PUSH k8s repo first, then annotate refresh. Commit a9ed8a8 (k8s), eaa3743 (symphony fork M4 code).
- NEXT M5: redka intents (IntentStore CRUD + /api/v1/intents + dashboard section + redka Tracker adapter w/ per-intent repo field), then WORKFLOW.md → tracker kind redka + agent.runtime pi_acp + seed intent → E2E dashboard→pi→journal→dashboard. Cluster pod→.147:18020 reachability test at E2E.

## M5 WIP (intents on redka) — code shipped, suite pending
- Redka DEPLOYED in cluster: deploy/redka/ (deployment+service+pvc; websites-private ArgoCD; commit f5d7fc6) — nalgeon/redka image, RESP on redka.default:6379 (10.110.1.109), sqlite /data/redka.db PVC.
- New symphony code: Intents.{Intent, IntentStore, RedkaConn}; Tracker.Redka adapter ("redka" kind registered); Tracker.notify_run_finished/3 OPTIONAL callback (function_exported guard — memory/github/etc unaffected); orchestrator calls it at the 3 journal_run_finished sites (completed→done; blocked/failed→failed; terminal intents left untouched). IntentStore backends: :redka (cluster, Redix conn w/ lazy reconnect + trap_exit) / :ets (tests; config/test.exs sets intent_store_backend :ets). Intent = hash intent:<id> + SMEMBERS intents; fields json-encoded where nested. App child between WorkflowStore & AgentRuntimeSupervisor (start_link returns :ignore for ets).
- Web: /api/v1/intents CRUD (IntentsApiController: index/create/show/cancel; routes BEFORE the :issue_identifier catch-all) + DashboardLive Intents section (register form phx-submit register_intent, cancel per-row, badge variants .state-badge-done, CSS added).
- Orchestrator loop semantics (learned): agent :normal exit → journal completed → complete_issue + CONTINUATION retry (re-fetch issue; terminal → stop+cleanup; still active → re-dispatch). So tracker MUST move issue terminal after run or it re-dispatches (memory-tracker churn). Retry w/ backoff capped by agent.max_retry_backoff_ms; failed intents marked terminal by notify → no infinite retry.
- GOTCHA (cost 30 min): my first mix.exs dep edit accidentally DELETED {:req}/{:jason} lines (marker-less payload) → 103-test blowup "Req.post undefined / module Req is not available" (deps compiled ok but missing from dep graph → not on code path). Restored. Lesson: verify deps list after mix.exs edits; when module "not available" at runtime w/ beams on disk → check mix deps output.

## M5 DONE (intents E2E verified in cluster) — symphony commits 1f50c71 + e218b75
- Full loop PROVEN: POST /api/v1/intents → redka (intent:<id> hash + SADD intents) → orchestrator (redka tracker kind, 2s poll) → dispatch → pi-acp → qwen3.8-27b vLLM (pod → 192.168.1.147:18020 works) executes job spec → turn → journal (session_started/turn_completed/run_finished completed/issue_terminal) → notify_run_finished → intent state done w/ result {status completed}. Dashboard shows Intents section (register form + done badge); cancel → cancelled; solid-render error → failed. done.txt content marker M5-E2E-V7-OK present in pi session.
- CONFIG KEYS for redka jobs on cluster WORKFLOW: tracker kind redka active ["open"] terminal ["done","failed","cancelled"]; agent.max_turns: 1 (single-turn job semantics; runner continues turns while issue ACTIVE so >1 spins); runtime pi_acp. Redka URL via REDKA_URL env redka.default:6379.
- pi models.json: qwen vLLM api MUST be "openai-completions" (NOT openai-chat-completions — pi 0.84.4: "No API provider registered"); baseUrl http://192.168.1.147:18020/v1; maxTokens 1024 (8192 → 6-9min/turn rabbit holes; qwen3.8-27b is a Qwen3 REASONING model — spirals on trivial tasks w/ big token budget); compat supportsDeveloperRole/supportsReasoningEffort false.
- Solid prompt templates are STRICT: {{issue.X}} must exist on Issue struct (no repo field! {{issue.repo}} crashes prompt render → run failed). Issue fields only: id/native_ref/identifier/title/description/priority/state/branch_name/url/assignee_id/blocked_by/labels/dispatchable/created_at/updated_at. WORKFLOW prompt should embed {{issue.title}}/{{issue.description}} so the job spec reaches the agent (else pi self-fetches from the symphony API).
- ArgoCD configmap volume sync + WorkflowStore hot reload lag ~60-90s: register a test intent AFTER the refresh settles or the dispatch uses the stale prompt.
- Image tags: 0.0.3-pilot-intents3 (final code) — k8s commits 0cf576e (redka deploy), 19c67e9/4621402/85c3dc2/fac1472 (symphony rollout/config). Redka deployment at deploy/redka (svc redka.default:6379, PVC sqlite).
- Post-reboot repairs: /tmp wiped (pi-cfg staging + /tmp/sym-home perms: chown via root alpine container; local.hex needed reinstall).

## Post-M5: dashboard E2E + hidden-reasoning disable (verified)
- Dashboard LOGIN: https://symphony.transparentlyrotatableproxy.site/ basic auth milady:milady (creds work in URL). Browser-verified: Intents form (phx-submit register_intent) registers intents live; websocket updates the table. Browser-registered "Browser smoke intent v3" completed through the pipeline (done).
- HIDDEN THINKING DISABLED for throughput: vLLM Qwen3 runs thinking by default (--reasoning-parser qwen3; 13s empty-content rabbit holes that ate token budgets). pi fix = models.json model entry: "reasoning": true + compat: {"thinkingFormat": "qwen-chat-template", ...} + settings.json defaultThinkingLevel "off" → pi sends chat_template_kwargs {enable_thinking: false, preserve_thinking: true}. Verified via sniff proxy (18021) + timing: cluster intent done in ~10-30s (was 2-4 min). compat.thinkingFormat is REQUIRED (model-level "thinkingFormat" alone is ignored by pi 0.84.4 openai-completions).
- EXPLICIT think TOOL: ~/.pi/agent/extensions/think.ts (pi.registerTool "think", thought param; returns tool_result so reasoning lands in history). Mounted in cluster via symphony-pi-config secret (think.ts key) + deployment subPath mount at .pi/agent/extensions/think.ts. WORKFLOW prompt nudges: "hidden reasoning disabled; call the think tool when you need deliberation". Verified: pi model called think then write (done.txt THINK-TOOL-OK), intent done.
- pi sniff technique: tiny http.server forwarding proxy on 18021 -> 18020 logging request bodies (models.json baseUrl point at it) — reuse for any pi payload debugging.

## Repo queue tooling (dashboard + scanner) — verified
- Intent state "queued" (non-dispatchable): dashboard Intents section = queue manager. Bulk "Queue repositories" textarea (URLs or owner/name, comma/newline); queued cards w/ per-row description input + Save task / Assign & run + Remove; history table excludes queued (queued badge warning).
- API: POST /api/v1/intents/:id/activate (queued->open), /assign ({"intent": {description}}; ?activate=true also opens). Store: activate_intent/park_intent/assign_intent/assign_and_activate_intent via transition_intent (from-state guard).
- scripts/queue_repos.py (symphony fork): scans ~/Documents siblings for any git remote w/ owner theycallmeloki; --queue registers each as queued repo intent ({"intent": {...}} envelope REQUIRED — 422 otherwise); --run opens; SYMPHONY_URL/USER/PASS env. 17 repos discovered (excluded llama.cpp/pachyderm forks via --exclude).
- Verified E2E: 16 queued via CLI; browser dashboard: queued cards render, per-row Assign & run on "Repo job: rum" -> open w/ assigned description -> done completed (~10s). 
- GAP (next): intent.repo is METADATA only — the pi agent gets an empty workspace and does NOT clone the repo. Real repo jobs need either a before_run clone hook per repo (hooks are static shell cmds; would need templating) or WORKFLOW rule + description instructing pi to git clone the repo URL first (github egress from cluster pod unverified).

## Dashboard UX overhaul (ux1→ux4) — verified live
- Intents & job queue now FIRST section under metrics; obs (rate limits/running/blocked/retry) moved below, collapsible (assign-backed toggle, default collapsed when empty, one-line summary). Success/error = colored notices (green/red), auto-dismiss 4.5s + × (was: everything rendered red error).
- Activity filter chips All/Open/Running/Done/Failed/Cancelled w/ live counts; newest-first; short ids `int-…suffix`; relative times ("2m 3s ago", title=absolute) — 1s tick re-renders keep them fresh.
- Forms: New job + Queue repositories side-by-side (2-col grid); disable-with on all submits; data-confirm on Cancel/Remove. Queue cards: no boilerplate prefill (queue_repo_intent now empty desc), 2-col grid ≥1180px w/ stacked card internals, responsive to 1-col row at ≤860-1180.
- Commits: symphony 70d6b7a (ux1 core), f37a81e (queue polish), b114a76 (2-col), c53a77d (stacked 2-col internals); k8s f5bba0a/d15d688/12a0a3d/19f9d87; images 0.0.3-pilot-ux{1,2,3,4}; live = ux4. mix test 330, only the 2 known CoreTest flakes.
- Verified in browser (real DOM + geometry): section order, chips filter (Done→8 rows), expand/collapse, queue-via-form "Queued 1 repo intent." green notice, queue-card Assign & run dispatch (gimmeapis done completed in ~24s), zero element clipping at 1440 (2-col) & 1000 (1-col) widths; OCR-verified clean cards.
- Gotchas: job_spec_changes API guard strips blank description values (can't clear task via API; UI Save path goes straight to store so clearing works there) — cleaned 15 stale boilerplate queue cards via browser Save-task loop. Browser element-selector screenshots work; fullPage shots in shared headless capture OTHER tabs (vision QA kept landing on hermes/qdrant pages) — use {selector} shots.
- 15 repos sit queued w/ cleared task fields awaiting assignment (rum+gimmeapis done via earlier runs).

## Cross-repo session: github-automation E2E + sweep + discussion
- github-automation = "Wine Automation": dumb single-step Jenkins job pi-runner (PROMPT → pi -p on Wine clone → BUG_ID.zip {prompt.txt, output.jsonl, changes.patch, pi.stderr, git-commit.txt}). changes.patch = OUTPUT ARTIFACT not a function: Capture Patch stage `git add -A; git reset -- build.log configure.log; git diff --cached --binary > changes.patch`; resume re-pins git-commit.txt + `git apply`. prompts/wine-*.md = pi role prompts (reproduce/root-cause/fix/test/commit/push). Makefile/jobs.py (delete→create w/ bootstrap) + trigger script w/ CSRF crumb; JENKINS milady:milady localhost:8080. NO woodpecker/GH-Actions in repo.
- E2E via dashboard (15:16Z): queued github-automation, task = clone+analyze+done.txt. Agent ran REAL terminal: clone failed `could not read Username` (private repo 404 w/o creds); probed GitHub API, verified repos/theycallmeloki/github-automation 404 + absent from 98 public repos; wrote CLONE-FAILED findings. Intent done completed ~2min, journaled. LEARNED: (1) cluster→github.com EGRESS WORKS; (2) private repos need credentials in the pi runtime (none today); (3) repo metadata never reaches agent (tracker/redka to_issue drops repo; workspace empty scratch, no clone).
- Sweep (3 scouts) key: MiladyOS = forge/woodpecker+kaniko build fabric + MCP, ArgoCD app-of-apps co-deploys sandman+kaniko; symphony = elixir obs+orchestrator (SSH/pi_acp, redka intents); sandman = Go P2P docker fabric (control 192.168.1.15:4242, NO kaniko/woodpecker link in code). Duplication: (a) repo-walking: symphony scripts/queue_repos.py vs generic walking; (b) github REST wrappers: symphony tracker/github/client.ex + agent_tool vs github-automation jenkins logic (thin); (c) job dispatch loops: sandman placement vs symphony orchestrator — orthogonal (docker vs ssh agents). Real gaps: intents repo never cloned; private repos unauth; no cross-repo message bus; sandman/symphony zero code cross-refs; MiladyOS Jenkins migrated→woodpecker already (09-01 memory).
- Ideas on table: genericize github-automation pattern (clone→agent→changes.patch→zip) into symphony repo-jobs (drop jenkins); changes.patch as universal agent-output contract → dashboard could fetch/apply patches to move items between repos; centralize repo walking; first-principles cross-repo comms.
- Decisions (discussion round, user): comms = Symphony hub (intents API + redka = control plane; woodpecker stays CI); repo access = LAN pre-clone cache (host mirrors, NO gh creds in cluster; runner clones from LAN); github-automation = FULL cutover now (drop jenkins/ + Makefile plumbing, wine-* prompts into pi runtime); dashboard patches = phase 1 artifacts (repo-jobs emit changes.patch + session; view/download in dashboard), apply = phase 2.
- User: implement bit by bit from the plan doc, not all in one turn → appended plan-symphony-pi-build-fabric.md w/ slices.
## sandman delta receiver (Cut 1 of cross-repo fabric) — LANDED a572e0f (pushed)
- User chose delta-overlay receiver (not literal changes.patch parser) + sandman store = the cache (no /srv/git-mirror/git daemon).
- What exists: git inputs = push receiver POST /api/v1/git/push {url,branch,revision,files,private} — webhook delivers FULL working tree; daemon never clones/creds (no git binary anywhere); mapped repos live in content-addressed store (commits w/ ancestry, views resolve parents, .git/HEAD = external revision marker); each push = 1 commit re-triggering bound pipelines (commitRevision → triggerForCommit).
- New: POST /api/v1/git/delta {url,branch,revision,base?,files,deleted,private} (git.go gitDeltaEvent/gitDeltaH/gitDelta + headRevisionMarker; gitBindings extracted from gitPush, shared). Semantics: deletions first, then files (files win on conflict), then .git/HEAD=revision; ONE commit per mapped repo (custom-name fanout) w/ push-identical private-fail + clear-recovery; base guard: base != head's recorded revision → no commit + pipeline fails "delta base …" (stale edit guard); matching-base delta or blind delta clears it; delta onto headless repo bootstraps partial revision (no base) / fails (base).
- Client: PushGitDelta(url,branch,revision,base,files,deleted,private). Route: git.delta. README git-inputs section updated.
- Tests: conformance/git_delta_test.go — 4 tests (apply+trigger, stale-base fail→recover, private fail, empty-repo bootstrap/base-reject) ALL PASS + existing 6 TestGit* pass (refactor regression clean); gofmt/vet/build clean; client+store+root unit sweep green. Toolchain: go at /home/laneone/sdk/go/bin (not on PATH).
- Cut 2 candidates (next): literal unified-diff changes.patch receiver (needs daemon-side hunk parser; text first); or export/read API for mapped head trees; or sender-side helper (CLI `sandman patch <dir> …` computing the delta from a git checkout) so the pi runtime loop closes.
## sandman patch verb (Cut 2) — LANDED 4f01213 (pushed)
- internal/cli/patch.go: `sandman patch [dir] [--url --base --branch --private]` — computes a real git checkout's worktree edits vs a base (default HEAD = uncommitted-edits mode; revision stays HEAD → constant base for agent loops; committed edits need --base <fork>) and POSTs /api/v1/git/delta. Files map = full content of changed+untracked paths (symlinks → target, text-only contract shared w/ push), deleted = removed. URL: --url or origin remote (ssh/scp/git:// → https rewrite). Never stages/commits/pushes. Registered in cli.Commands() + main.go usage; README git-inputs para + verb list updated.
- Bug caught by live E2E: buildPatchDelta resolved URL internally but didn't return it → verb sent url="" (daemon 200 no-op). Fixed (url returned).
- E2E (scratch daemon :4299, process runner): pipeline edit-demo w/ git input https://github.com/example/delta-e2e.git; seed full push (rev=head0); edit README v2 + rm lib/x.go + add new.txt → `sandman patch` → mapped repo head: README=v2, new.txt=fresh, lib/x.go gone, .git/HEAD=head0; exactly 2 jobs success. Loop closed: git checkout → patch → delta commit → pipeline trigger.
- E2E hazard discovered (sandbox spec bug, not product): pipeline NAMED same as git-input mapped repo (URL-derived) → output commits land in own input repo → self-feedback runaway (217 commits). sandman HAS SelfReferentialInputRejected for repo inputs but git-derived names collide post-derivation; possibly a real gap to note (not fixed).
- Tests: internal/cli/patch_test.go 4 tests (uncommitted edits incl. delete+untracked, committed w/ --base, no-change no-op, https url rewrite) + full sweep green. Toolchain PATH note: go at /home/laneone/sdk/go/bin.
- Plan state: R2 clone-step sibling done conceptually (delta receivers native); next candidate: wire pi/symphony to push deltas (G1 genericization), or git-input self-reference guard, or diff view of mapped repo (GET tree compare for dashboard artifacts).
## G1 runtime delta slice (symphony code) — LANDED 20b478d (pushed)
- Issue struct gains :repo (nil default); tracker/redka to_issue now carries intent.repo (was dropped → url nil). Other adapters unaffected.
- NEW SymphonyElixir.RepoDelta (lib/.../repo_delta.ex): inert unless SANDMAN_ADDR env + intent.repo set.
  - bootstrap/3 (local worker only): mapped repo name from URL (repo_name), GET sandman /repos/{name}/branches/{branch}/head → commit id + .git/HEAD marker revision; list files (excl .git/*); fetch each (per-segment URI-encoded path); write_tree; git init+config+add+commit base ("materialized url @ rev"); append .sandman-src to .gitignore; write marker .sandman-src {url,branch,revision}. Repo not mapped → {:error,:not_mapped} → workspace stays empty (previous behavior), run unaffected.
  - emit_delta/3 post-run: only when marker present; git rev-parse HEAD; worktree_delta = git diff --name-status -z --no-renames HEAD + untracked (ls-files --others --exclude-standard; .gitignore excludes marker); POST {url,branch,revision:HEAD,base:markerRev,files,deleted,private:false} to /api/v1/git/delta; base=marker revision passes daemon staleness guard; revision=workspace HEAD advances mirror. {:ok,:no_changes} when clean.
  - Best-effort wrappers log, never raise/fail runs. Remote (ssh worker_host) unsupported → log skip.
- agent_runner: after workspace create → RepoDelta.best_effort_bootstrap; in `after` (after run_after_run_hook) → best_effort_emit. PromptBuilder: append_repo_context block when issue.repo — tells agent workspace holds checkout, no push/commit (creds none), edits captured post-run (template-independent).
- Fixed: read_marker returned inner map not {:ok,map} (caught pre-test); bootstrap clause ordering.
- Tests: repo_delta_test.exs 8 (repo_name, parse_statuses changed/deleted/vanished/empty, write_tree nested, marker roundtrip/missing/malformed) + redka 9 pass; FULL suite 338 tests 2 failures = the 2 known CoreTest timing flakes; mix compile --warnings-as-errors clean; mix format applied. Env: elixir tests run via docker elixir:1.19.5-otp-28 (no local toolchain) w/ -v elixir mount.
- Deploy surface next (NOT done): SANDMAN_ADDR (+ optional SANDMAN_DEFAULT_BRANCH) env on symphony deployment (self-hosted-k8s-private repo commit + ArgoCD), image build 0.0.3-… tag + k8s tag bump, then live E2E: scratch sandman daemon on this box .147:PORT w/ git-input pipeline binding a real org URL + seeded mirror, queue repo intent, agent edits, verify delta commit lands + pipeline job. Runtime image already has git (Dockerfile apt git).
- NOTE: symphony Dockerfile working tree had PRE-EXISTING uncommitted pi-runtime layer change (git log last Dockerfile commit 10fda90) — left untouched; images built from it locally.
## G1 deploy + module E2E — DONE
- k8s (self-hosted-k8s-private): deployment env SANDMAN_ADDR=http://192.168.1.15:4242 (live control plane; inert until git-input pipelines exist — mapped_head :not_mapped → empty workspace fallback). Images: 0.0.3-pilot-g1 (first), then g2 after two fixes; commits 343be44 → 3f5e9d0; live = g2, rolled out. Symphony commits: 20b478d (feature), cad7fdd (git with-clause fix: git_ok returns {:ok, out} not :ok), 393749c (best_effort rescue crashes too — bootstrap/emit can never fail an agent run).
- Module E2E (real sandman scratch daemon :4298, process runner): pipeline ga-watch w/ git input https://github.com/theycallmeloki/github-automation.git; seeded full tree (17 files, rev=head0 of local checkout). Elixir container --network host, SANDMAN_ADDR=127.0.0.1:4298, mix run --no-start + Application.ensure_all_started(:req) (full app boot needs provider tokens — avoided). RepoDelta.bootstrap → :ok, 5 spot files materialized, marker revision=head0. Simulated agent edits (README append, new scripts/notes.txt, delete jenkins/trigger-pi-runner.sh) → emit {:ok,:delivered} 2 changed/1 deleted. Verified daemon: head tree README has edit, notes.txt present, trigger script 404, .git/HEAD marker = workspace base sha (c92d2dc…), ga-watch jobs == 2 both success. FULL G1 LOOP PROVEN against real sandman.
- Open decision for true pod-level E2E: add a git-input pipeline (e.g. ga-watch) to the LIVE .15 sandman + seed mirror there (mutates live fleet — user call). Until then live SANDMAN_ADDR is inert/no-op.
- Runtime image has git (Dockerfile apt). elixir toolchain = docker elixir:1.19.5-otp-28; mix compile/test inside w/ local.hex --force; elixir/tmp gitignored.
## Build-bus (MiladyOS side) — VALIDATED, live watch+promote pending
- Decisions: sandman watch pipeline detector (event-driven), KanikoBuild CR path, first target symphony, git-ops auto-promote.
- Fabric: forgejo .147:3000 (milady:milady basic), woodpecker .147:8000 (token /var/lib/woodpecker/.secrets in miladyos container), repos milady/{ad-hoc,kaniko,buildbus}. kaniko-submit:1 image (entrypoint kaniko-submit.py), CRD kanikobuilds.build.miladyos.io + ns sandman + hook work (earlier pilot images kaniko-built: kb-symphony-0.0.3-pilot etc).
- NEW milady/buildbus repo (forge id 3, wp repo 3): .woodpecker.yml = woodpecker/build-bus.yml (canonical copy in MiladyOS repo). Steps: fetch-context (python alpine: pulls mapped repo tree from SANDMAN_ADDR at REVISION into src/, computes REGISTRY/IMAGE:<rev12>, skip-file if tag in authless tags/list) → kaniko-build (kaniko-submit:2, kubeconfig_content secret) → promote (alpine/git, GITHUB_TOKEN secret, PROMOTE=1 → clone self-hosted-k8s-private, regex-bump image line deploy/symphony/deployment.yaml, commit+push main). Secrets on repo: kubeconfig_content (from ~/.kube/config; events ARRAY required), GITHUB_TOKEN (from k8s secret symphony-github-token). Woodpecker: secret events field = list; config generation FAILS if referenced secret missing (github_token not found) → add secrets before triggering.
- BUGS FOUND+FIXED: (1) kubectl client-side apply last-applied annotation ≤262144 → context CM b64 >~250KB rejected (symphony 148 files = 198KB raw/264KB b64). Fix: kaniko-submit.py apply → --server-side=true (both CM + CR sites) → rebuilt image :2. (2) registry skip via manifest HEAD fails authless (404 even when tag exists); tags/list is authless 200 → skip check moved into fetch step (kaniko-submit's internal skip ineffective from step containers). Rerun same rev: 8s skip success.
- PROOF: pipeline 3 success — fetch 148 files from scratch sandman mirror (172.17.0.1:4297 seeded from local symphony checkout tracked files @393749c-era + uncommitted pi-layer Dockerfile), kaniko CR kb-symphony-latest-20260902163444 Succeeded → miladyosregistry/symphony:6ddb1b3e59cc pushed. Rerun (same rev) → registry-skip 8s.
- MiladyOS commit 24a7e81 (build-bus.yml new, kaniko-submit.py fix, kaniko-build.yml image :2). Image kaniko-submit:2 pushed (docker create/cp/commit from :1).
- TODO (needs live-infra go-ahead): (a) wire sandman git-input watch pipeline on LIVE .15 for symphony URL → job curls woodpecker POST w/ vars (token as sandman secret binding); (b) PROMOTE=1 live test (bumps k8s repo deploy/symphony image → ArgoCD rolls; image 6ddb1b3e59cc content ≈ current g2). Note: symphony image Dockerfile currently includes uncommitted pi-runtime layer change (working tree) — builds from mirror include it (matches deployed g2).
## Build-bus LIVE + auto-deploy VERIFIED (watch + promote)
- Detector LIVE on .15: pipeline "symphony-watch" (git input https://github.com/theycallmeloki/symphony.git, transform python:3.12-alpine POSTs woodpecker repo 3 pipelines w/ SANDMAN_ADDR/REPO_URL/IMAGE/PROMOTE=1; sandman secret wp-bus (WOODPECKER_TOKEN) via transform secret binding envVar).
- Seeded live mirror: per-file commit 144 text + commit 2 adding 4 binary assets (favicon.png needed at compile by static_assets.ex — 4 binaries were load-bearing; first kaniko build failed on missing favicon). Head e751aba04ba194be.
- Full chain verified: seed commit → watch job → pipeline 11 (fetch+skip since tag existed after p7? sequence: p7 kaniko SUCCESS built e751aba04ba1 (after assets commit) but promote FAILED (braces stripped → empty clone URL); p11 promote SUCCESS (sed version, after stub/bisect — python heredoc caused "unable to parse variable name", replaced w/ sed) → commit 4455713 → sed dropped `image:` key → ArgoCD ComparisonError yaml L26 → fix commit 98d31ff restored key → rollout symphony:e751aba04ba1 Available, dashboard 401. 
- Gotchas banked (plan doc): secret events array; missing secret fails config gen; ${} stripped (braces banned — defaults via [ -z ] tests, sed \1 kept); heredoc must be block-indented (stub bisect isolated parse error to the python heredoc); kaniko-submit --server-side=true for big contexts (annotation 256KB cap); registry skip only via authless tags/list.
- MiladyOS commits: 24a7e81 (bus+kaniko fixes, image :2), e1c861f (skip + brace-free sed promote). k8s repo: 4455713 (auto image bump), 98d31ff (image-key fix). symphony watch pipeline lives on .15 daemon (not in a repo — recreate via /tmp? note: spec pattern saved in this memory? record: python3 -c POST w/ env from sandman secret wp-bus; spec fields name symphony-watch image python:3.12-alpine secrets [{name wp-bus key token envVar WOODPECKER_TOKEN}], env BUILDBUS_ID 3 etc).
## sandman v0.2.44 release + fleet roll (delta receiver LIVE)
- Live .15 daemon was v0.2.43 (pre-delta) → cluster RepoDelta emit 404'd (channel dead in prod). Recon: release flow = GitHub release (theycallmeloki/sandman, assets sandman-linux-{amd64,arm64}[.sha256], checksum-verified `sandman update` self-install to /usr/local/bin/sandman); fleet roll = sandman-pipelines/roll-update.sh (manifest: miladyos-42 .147/02 .133/03 .32/04 .241 workers, miladyos-sandmand .15 daemon LAST; sudo -S PASS; k8s phase: docker build Dockerfile.worker → push ogmiladyloki/sandman:worker-v<ver> → sed deploy/k8s/hook-configmap.yaml workerImage → commit+push → argocd refresh app sandman → hook deploy restart → decorator recreates worker pods).
- Release v0.2.44 (delta receiver + patch CLI; built 4f01213; 2 commits past 918271c) tag+assets uploaded. This box = miladyos-42 (.147); NOPASSWD fleet except .147 (sudo pass = "l" — used once, NOT persisted).
- Roll: all 5 nodes → v0.2.44 (daemon restarted last); k8s worker image worker-v0.2.44 pushed + hook commit 7e7861e; 3 healthy talos worker pods (3hl-zyf .168, ga5-yk4 .97, ugl-4xf .160) 2/2 Running worker-v0.2.44. talos-ms4-c7v (.126) NotReady/SchedulingDisabled SINCE 2026-09-01 (env, pre-existing) — its worker pod stuck Terminating 30h; force-deleted; decorator recreated (Pending, new image) — resolves when node returns.
- Verified live: .15 version v0.2.44; POST /api/v1/git/delta {} → 200 (route live; malformed → 400); repos/pipelines intact (symphony 31MB head e751aba04ba1, watch/spout/gh-webhook running). Empty {} delta = no-op (no spurious commit/bus fire).
- NEXT for full cluster→bus E2E: promote step needs no-op guard (DEST unchanged → git commit fails w/ set -e → pipeline error; skip commit when git status clean).
## FULL E2E VERIFIED: RepoDelta → live mirror → watch → bus → auto-deploy (52e7c021919d)
- Precondition discovery: live mirror head (seeded via per-file API) had NO .git/HEAD tree marker → headRevisionMarker = ("",true) → ANY based delta FAILS guard silently (receiver 200s "ok", marks watch pipeline failure "delta base"). Fix: blind delta (base omitted) stamps .git/HEAD marker (= e751aba04ba194be) + advances head (ca08f8c9; .git-only commit = 0 datums → no job, by DAG law). Pipeline failure auto-cleared on head advance.
- Driver: elixir:1.19.5-otp-28 container (deps cached in mounted elixir/, hex per-container: mix local.hex --force) w/ SANDMAN_ADDR=http://192.168.1.15:4242; mix run --no-start /tmp/rd_e2e.exs → RepoDelta.bootstrap /ws (Issue{repo: github symphony URL}) → append README marker → emit_delta {:ok,:delivered}.
- Chain: mirror head e751aba→52e7c021919dd217; watch job 7ceaa70b3ec8 success; woodpecker pipeline 13 fetch+kaniko(real, ~4.5min)+promote success; registry tag symphony:52e7c021919d; k8s repo commit e1c3978 (image bump); ArgoCD websites-private → rollout symphony:52e7c021919d; dashboard 401, Available.
- Also: MiladyOS f0d8065 = promote no-op guard (skip commit when already at DEST; pipeline 12 verified).
- Lesson: RepoDelta emits are based deltas — every mirror that will receive them needs its .git/HEAD marker stamped (one blind delta) OR bootstrapped deltas fail. A full git-side push stamps it automatically.
## ms4 recovered + nicety + bus-wire bake-in
- talos-ms4-c7v back (user fixed): node Ready, worker pod 2/2 worker-v0.2.44, fleet 4/4 (want=4 got=4 notready=0 oldimg=0).
- sandman nicety (markerless-head delta base): headRevisionMarker now returns head.ID when head tree has no .git/HEAD marker — base == head id accepted (RepoDelta bootstrap fallback commits w/o a stamp). Docstrings updated; new conformance TestGitDeltaMarkerlessHeadRecordsItself (per-file seeded head: stale base refused, base==head commits+clears). All TestGitDelta* green. COMMITTED (git.go + test) → release v0.2.45 pending on push + roll-update.
- bus-wire bake-in: sandman-pipelines tools/bus-wire.py (commit 436d3a6) — canonical copy of live symphony-watch v1 (byte-identical trigger python verified), creates/version-bumps the watch pipeline + stamps .git/HEAD marker when missing (blind delta; no watch job). Validated spec parity vs live (transform cmd byte-identical, input/env equal; live head marker present → no stamp needed).
- Plan doc restructured: Status ledger (done/live) + Open items (1 release v0.2.45; 2 RunJournal build/job fusion; 3 H1 hub consolidation; 4 wire next bus repo via bus-wire.py). Stale Gaps/Milestones/Slices/E2E lists replaced w/ historical notes.
## sandman v0.2.45 release + roll + roll-update builtin-registry flip
- v0.2.45 released (tag b7c574f: markerless-head nicety — headRevisionMarker returns head.ID when tree lacks .git/HEAD; TestGitDeltaMarkerlessHeadRecordsItself added; full suite EXIT 0). Fleet nodes → v0.2.45, daemon .15 verified.
- roll-update.sh now pushes the k8s worker image to the BUILTIN miladyosregistry (authless push verified; anonymous DELETE is 405 so stray probe tag worker-v0.2.45-probe remains in registry — harmless) instead of ogmiladyloki dockerhub (aligns w/ no-dockerhub directive; talos workers pull authless in-cluster). Commits 60370ed + b7fbff5 (b7fbff5: convergence checks FULL image ref — the tag-only check let the registry flip pass while pods still ran the dockerhub ref).
- Roll end state: hook CM pin a966255 = miladyosregistry.../sandman:worker-v0.2.45; 4/4 worker pods Running on miladyosregistry ref, 0 dockerhub refs; daemon v0.2.45. NOTE: a mid-run edit of roll-update.sh while /bin/sh executes it causes a tail "Syntax error" — harmless when the roll already finished (sh reads incrementally); edit between runs, not during.
