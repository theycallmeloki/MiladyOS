# 2026-08-31

## Clean slate (operator-directed)
- `sandman reset --yes` — repos/pipelines/jobs/secrets/tags wiped (spec repo recreated). Backup taken first: `/tmp/sandman-pre-reset.tar.gz` (container). Daemon had NO secrets anyway.
- `kubectl delete kanikobuilds --all -n sandman` — 36 CRs gone, 0 left. Jenkins wave had already drained (0 running/queued).

## MCP direction change (operator: "talk directly to the mcp server without making changes to the mcp sse server itself")
- **Reverted `run_jenkins_job` tool** from miladyos_mcp.py (back to 18 tools). Triggering now goes through the MCP's existing `execute_command` (crumb'd curl POST), not server code. Commit `7c72d06` (MiladyOS).
- **kubeconfig via Jenkins credential, no volumes** (bondman/raw pattern: `--kubeconfig=$WORKSPACE/.kube/config`): created String credential `kubeconfig` (cluster admin config, StringCredentialsImpl via `json=` form param — the plain form-encoded create 400'd; `json={"": "0", "credentials": {...}}` → 302 = success). All 51 build templates got a `Kubeconfig` stage: `withCredentials([string(credentialsId:'kubeconfig', variable:'KUBECONFIG_CONTENT')])` → `printf "%s" "$KUBECONFIG_CONTENT" > ${WORKSPACE}/.kube/config` + `env.KUBECONFIG`. builder.sh volume mounts removed. Commit `90b1d50` (sandman-pipelines).

## Repo consolidation
- bondman/raw (117 Jenkinsfiles) → `bondman-raw/` then folded into `templates/` (75 unique join 52 canonical; colliding names already represented). **23 files scrubbed** of live creds (ghp_ PAT, B2 keys, Mostwanted1) → CHANGE_ME_* placeholders before push. Commits `59f93b3`, `50395c0`.
- **762 x402 endpoint templates** added from `/home/laneone/Downloads/agentutility/x402-endpoints.jsonl` (agentutility = paid x402 endpoints, USDC on Base/Solana, MCP-callable). Each: name (from path), path, method, version, tags, legacyPrice {price, price_usd, price_lt}, description — header comment + Write-Context spec.json scaffold. Commit `54e8989`. Templates/ now 888 total.

## x402 facts (agentutility)
- Base URL `https://x402.agentutility.ai/<path>`. POST → 402 + `payment-required` base64 header (x402Version 2, accepts[] with eip155:8453 USDC + solana USDC exact-scheme routes, 300s timeout).
- OpenAPI at `/openapi.json` — per-endpoint schema + `x-price-usd`.
- SDK: `x402@1.2.0` npm is DEPRECATED v1; v2 = `@x402/fetch` (wrapFetchWithPayment + x402Client + ExactEvmScheme from `@x402/evm` / ExactSvmScheme from `@x402/svm`). Payment deferred per operator.

## youtube-dl E2E green on clean slate (the reference test)
- Trigger gotcha: **fresh jobs 400 on buildWithParameters** ("not parameterized" — params materialize after first build) → use plain `/build` (defaults). Also: **create_jenkins_job wipes build history + immediate trigger races** → wait, then trigger.
- **Registry-skip guard** added (operator: "if the container already existed we shouldn't do the build at all"): Build stage curls `https://<registry>/v2/<name>/tags/list`, skips kaniko CR + wait loop if tag exists. Commit in youtube-dl.
- Dead URLs (documented): `youtube.com/watch` → nsig/player-API 400 fight; gtv-videos-bucket BigBuckBunny → 403; w3schools mov_bbb.mp4 → 403. Working direct mp4s: test-videos.co.uk, filesamples.com, MDN `interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4`.
- `-x` alone → m4a (codec passthrough); `--audio-format opus` → deterministic `downloaded_video.opus`. Default YOUTUBE_URL updated to MDN flower.mp4.
- Result: build SUCCESS (skip path), sandman job success, `downloaded_video.opus` (1518 B, OggS magic) committed to youtube-dl@master + fetched. Commits: guard `90b1d50`→`(unpushed at write time)`.

## ability-check (divergent experiment, operator-questioned)
- Built a local-transform probe template (d20 per description, deterministic seed, contract probe) — operator: "I feel like thats wrong sort of maybe", pivoted to youtube-dl E2E instead. Template committed but NOT the direction; x402 payment wiring still open.

## Open items
- spread registry-skip guard to the other 50 build templates
- x402 wallet (Base key w/ USDC or Solana key) when we wire payments
- ability-check template direction (local probe vs paid call)
- roll-update.sh/standup.sh fate; Nebula CA rotation (pre-existing)
