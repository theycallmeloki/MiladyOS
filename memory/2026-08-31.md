# 2026-08-31

## Clean slate (operator-directed)
- `sandman reset --yes` — repos/pipelines/jobs/secrets/tags wiped (spec repo recreated). Backup first: `/tmp/sandman-pre-reset.tar.gz` (container). Daemon had NO secrets.
- `kubectl delete kanikobuilds --all -n sandman` — 36 CRs gone. Jenkins wave had drained.

## MCP direction (operator: "talk directly to the mcp server without making changes to the mcp sse server itself")
- Reverted `run_jenkins_job` tool (18 tools, commit 7c72d06). Triggering via MCP `execute_command` (crumb'd curl). kubeconfig via Jenkins String credential `kubeconfig` (StringCredentialsImpl, json= form param, 302=success); 51 templates got a Kubeconfig stage (withCredentials → $WORKSPACE/.kube/config + env.KUBECONFIG); builder.sh volume mounts removed (90b1d50).

## Repo consolidation
- bondman/raw 117 files → templates/ (75 unique; 23 files scrubbed of live creds → CHANGE_ME_*). 59f93b3, 50395c0.
- 762 x402 endpoint templates from Downloads/agentutility/x402-endpoints.jsonl (agentutility = paid x402 endpoints, USDC Base/Solana, MCP-callable). Name from path; path/method/version/tags/legacyPrice{price,price_usd,price_lt}/description. 54e8989. templates/ = 888 total.
- x402 facts: base https://x402.agentutility.ai/<path>, POST→402+payment-required (v2, eip155:8453 USDC + solana exact routes, 300s). openapi.json per-endpoint. SDK: v1 `x402` npm deprecated; v2 `@x402/fetch` (wrapFetchWithPayment + x402Client + ExactEvmScheme/SvmScheme). Payment deferred per operator.

## youtube-dl E2E green on clean slate (reference test)
- Fresh jobs 400 on buildWithParameters ("not parameterized" until first build) → plain /build (defaults). create_jenkins_job wipes history; trigger after a beat.
- Registry-skip guard: Build stage curls tags/list; image exists → skip kaniko CR + wait (operator: "if the container already existed we shouldn't do the build at all").
- Dead URLs: youtube watch (nsig 400), gtv-videos-bucket + w3schools (403). Working direct mp4: MDN flower.mp4. `--audio-format opus` → deterministic downloaded_video.opus (OggS, 1518 B) committed to youtube-dl@master. Build SUCCESS (skip path), sandman job success.

## MCP cleanup (operator: "see if we're actually using all the tools and drop it if its not needed")
- **18 → 7 tools**: dropped create_template, edit_template, list_templates, view_template, deploy_pipeline, run_pipeline, get_pipeline_status, list_pipeline_runs, read_query, list_db_tables, describe_db_table (0 usage evidence: no repo refs, redka EMPTY — 0 keys).
- **Kept**: hello_world (operator: "it has to respond milady!"), create_jenkins_job, execute_command + ALL AlphaEvolve (operator: "keep all the alpha evolve stuff").
- Removed: SqliteApiError/SqliteUtils, TemplateUtils, metadata_manager import (comments-only), sqlite3/closing imports, Config.SQLITE_DB_PATH + METADATA_DIR, main.py sqlite/metadata options. Kept Config.TEMPLATES_DIR (JenkinsUtils + evolve_template). miladyos_mcp.py 2760 → ~1270 lines.
- **execute_command runs via Jenkins** (CLI Experimenter job, command embedded by string replace). Post-recreate bug: /workspace + @tmp AccessDenied → default WORKING_DIR '/tmp/workspace', replace quotes dir arg. Smoke: SUCCESS.
- Restart gotcha: `kill $(pgrep -f "main mcp")` self-SIGTERMs wrapper (cmdline contains literal "main mcp") → use `pgrep -f "main mc[p]"` + separate start exec.
- Docs: apis/_index.md 18→7 (+create_jenkins_job section), pipeline-templates Template Management rewritten, getting-started fixed.
- Commit 7ae8c13.

## TempleOS: ISO/QEMU → templeos-loader (operator: "get rid of the TempleOS.iso, use the loader and run .hc files")
- **templeos-loader** (theycallmeloki/templeos-loader) runs TempleOS V5.050 kernel in Linux userspace — no QEMU, no ISO. HolyC scripts talk stdio via vsyscalls: `VSysCall(9,0,&ch,1,0)` = VSYSCALL_READ (stdin), `"..."` → RawPutChar → PUTCHAR (stdout).
- **Fixed upstream (committed f64a520, pushed)**: (1) mainstatic.c `CompilerInit()` decl — gcc 15 implicit-decl error; (2) elf64-x86-64.ld explicit `PHDRS { text PT_LOAD FILEHDR PHDRS FLAGS(7); }` — binutils ≥2.39 `-z separate-code` × `--omagic` dropped the ELF header/phdr table out of the first LOAD → musl `__init_tls` SIGSEGV reading AT_PHDR (boot crash on modern toolchains). Build: musl v1.2.1 → PhysFS → `cmake --build . --target templeoskernel`.
- **Boot .HC gotchas**: needs `#include "::/AdamInit"` (kernel+compiler headers for Print/PutChars); top-level var decls rejected ("Use Asm Blk") → locals inside function; `VSYSCALL_READ` macro not in boot include chain → use raw `9`; declare `public _extern _VSYSCALL I64 VSysCall(...)` yourself; STARTOS=drive-letter path `D:/MiladyOracle.HC` (no leading slash); `--drive=C,MiniSystem --drive=D,<scripts>,<writedir>`.
- **Dockerfile**: TempleOS section rewritten — clone loader @f64a520, build musl/physfs/kernel, install `/opt/templeos/{templeoskernel,MiniSystem,scripts}`, `/usr/local/bin/templeos` launcher (exec env STARTOS=... --drive=C... --drive=D...), daemon wrapper. QEMU/NoVNC/websockify removed from apt+image; ISO download gone.
- **templeos/MiladyOracle.HC**: COM1 serial + PC speaker → stdio protocol (TEMPLEOS_READY, MILADY→MILADY!+RNG, RNG_REQUEST→RNG:n, PING→PONG, ECHO, TEMPLEOS_SHUTDOWN on EOF).
- **milady_oracle.py**: TempleOSBridge = subprocess (spawn templeos, pipes), QMPController dropped, config templeos_bin/boot_timeout, main() args updated.
- **startup.sh**: HOLY MISSION checks templeoskernel + MiladyOracle.HC + templeos launcher; oracle spawns loader.
- **get_divine_rng MCP tool ADDED (8 tools total, nothing removed — operator: "we need all of the previous ones, just add thi one")**: registry + branch spawn the loader, send RNG_REQUEST per count, return `rng` values. Verified over SSE: `{"rng": 2045364038435673789, "count": 1, "source": "TempleOS (templeos-loader)"}`. RNG is `RandU64` — hardware-timing entropy, one per RNG_REQUEST; count param just sends N requests.
- Verified: docker test image (TempleOS section only) boots + answers MILADY/RNG/PONG; full stack E2E in image; MCP 8 tools live.

## Open items
- spread registry-skip guard to the other 50 build templates
- x402 wallet (Base key w/ USDC or Solana) when wiring payments
- ability-check template direction (local probe vs paid call) — committed scaffold, operator questioned
- roll-update.sh/standup.sh fate; Nebula CA rotation (pre-existing)

## ISO work (evening session — STOPPED mid-flight, resume next turn)
- ISO/PLAN.md committed (7de9382): Debian 13 live-build + Calamares, k3s --docker,
  manual role + role-switch + Avahi worker-join + keepalived VIP (D1-D4 ruled).
- Uncommitted ISO/ scaffold (all untracked): build.sh (dockerized live-build,
  cache via .cache/ bind mount), auto/config (trixie, serial console, no quiet),
  builder/Dockerfile, config/ (package lists: docker-ce, k3s via get.k3s.io,
  open-iscsi, avahi, keepalived; hooks 1100-docker, 1200-k3s, 1300-firstboot,
  1350-dev-ssh), firstboot/ (role-detect, role-switch, discover-master,
  ensure-image, miladyos-container, gpu-detect, hostname-banner),
  systemd/ (miladyos-role.service, miladyos-container.service, avahi
  kubernetes.service, docker-daemon.json vfs, k3s network drop-in),
  qemu-smoke.sh (headless serial marker test), qemu-dev.sh (interactive VM).
- VERIFIED in QEMU (KVM, miladyos-7de9382.iso 1.7GB no-payload):
  boots → role=server applied (console marker) → docker up (vfs storage fix for
  tmpfs overlay) → k3s starts detached (network-online drop-in — live boot never
  completes that target) → BUT node-token never written in 300s (k3s server
  init incomplete — next blocker).
- FIXED so far: auto/config not executable (bootappend ignored), isolinux
  timeout 0 (menu hang), blocking systemctl enable --now on Type=notify k3s
  units (TimeOutStartSec=0 → infinite wait), set -e killing poll loop
  ([ -n ] && break), daemon.json with comments (invalid JSON), overlay upperdir
  on tmpfs → docker vfs, sshd host keys missing (baked via ssh-keygen -A in
  1350 hook + socket-only activation).
- Dev-loop pivot (operator): stop build→smoke cycles; boot ONE QEMU VM with
  serial+SSH, fix inside live overlay, bake back once. SSH into dev VM NOT yet
  working (kex reset — host keys were missing; baked now, untested).
- OUT dir + .cache are root-owned (docker) — clean with docker alpine chown.
- Next: commit ISO/ scaffold (or continue dev), verify sshd, then k3s token,
  then agent join, then payload embed, then Calamares.
