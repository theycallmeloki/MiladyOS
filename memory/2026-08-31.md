# 2026-08-31

## Clean slate (operator-directed)
- `sandman reset --yes` — repos/pipelines/jobs/secrets/tags wiped (spec repo recreated). Backup first: `/tmp/sandman-pre-reset.tar.gz` (container). Daemon had NO secrets.
- `kubectl delete kanikobuilds --all -n sandman` — 36 CRs gone. Jenkins wave had drained.

## MCP direction (operator: "talk directly to the mcp server without making changes to the mcp sse server itself")
- Reverted `run_jenkins_job` tool (18 tools, commit 7c72d06). Triggering via MCP `execute_command` (crumb'd curl). kubeconfig via Jenkins String credential `kubeconfig` (StringCredentialsImpl, json= form param, 302=success); 51 templates got a Kubeconfig stage (withCredentials → $WORKSPACE/.kube/config + env.KUBECONFIG); builder.sh volume mounts removed (90b1d50).

## Repo consolidation
- bondman/raw 117 files → templates/ (75 unique; 23 files scrubbed of live creds → CHANGE_ME_*). 59f93b3, 50395c0.
- 762 x402 endpoint templates from Downloads/agentutility/x402-endpoints.jsonl (agentutility = paid x402 endpoints, USDC Base/Solana, MCP-callable). Name from path; path/method/version/tags/legacyPrice{price,price_usd,price_lt}/description. 54e8989. templates/ = 888 total.
- x402 facts: base https://x402.agentutility.ai/<path>, POST→402+payment-required (v2, eip155:8453 USDC + solana exact routes, 300s). openapi.json per-endpoint. SDK: v1 `x402` npm deprecated; v2 `@x402/fetch` (wrapFetchWithPayment + x402Client + ExactEvmScheme/SvmScheme). Payment deferred per operator.

## youtube-dl E2E green on clean slate (reference test)
- Fresh jobs 400 on buildWithParameters ("not parameterized" until first build) → plain /build (defaults). create_jenkins_job wipes history; trigger after a beat.
- Registry-skip guard: Build stage curls tags/list; image exists → skip kaniko CR + wait (operator: "if the container already existed we shouldn't do the build at all").
- Dead URLs: youtube watch (nsig 400), gtv-videos-bucket + w3schools (403). Working direct mp4: MDN flower.mp4. `--audio-format opus` → deterministic downloaded_video.opus (OggS, 1518 B) committed to youtube-dl@master. Build SUCCESS (skip path), sandman job success.

## MCP cleanup (operator: "see if we're actually using all the tools and drop it if its not needed")
- **18 → 7 tools**: dropped create_template, edit_template, list_templates, view_template, deploy_pipeline, run_pipeline, get_pipeline_status, list_pipeline_runs, read_query, list_db_tables, describe_db_table (0 usage evidence: no repo refs, redka EMPTY — 0 keys).
- **Kept**: hello_world (operator: "it has to respond milady!"), create_jenkins_job, execute_command + ALL AlphaEvolve (operator: "keep all the alpha evolve stuff").
- Removed: SqliteApiError/SqliteUtils, TemplateUtils, metadata_manager import (comments-only), sqlite3/closing imports, Config.SQLITE_DB_PATH + METADATA_DIR, main.py sqlite/metadata options. Kept Config.TEMPLATES_DIR (JenkinsUtils + evolve_template). miladyos_mcp.py 2760 → ~1270 lines.
- **execute_command runs via Jenkins** (CLI Experimenter job, command embedded by string replace). Post-recreate bug: /workspace + @tmp AccessDenied → default WORKING_DIR '/tmp/workspace', replace quotes dir arg. Smoke: SUCCESS.
- Restart gotcha: `kill $(pgrep -f "main mcp")` self-SIGTERMs wrapper (cmdline contains literal "main mcp") → use `pgrep -f "main mc[p]"` + separate start exec.
- Docs: apis/_index.md 18→7 (+create_jenkins_job section), pipeline-templates Template Management rewritten, getting-started fixed.
- Commit 7ae8c13.

## Open items
- spread registry-skip guard to the other 50 build templates
- x402 wallet (Base key w/ USDC or Solana) when wiring payments
- ability-check template direction (local probe vs paid call) — committed scaffold, operator questioned
- roll-update.sh/standup.sh fate; Nebula CA rotation (pre-existing)
