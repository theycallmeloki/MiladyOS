[Unit]
# sshd must never race the first-boot host-key generation (live boots
# have no baked keys; a connection attempt before keygen leaves sshd
# failing with "no hostkeys available"). Socket-activated start waits
# for this After.
After=milady-sshkeys.service
