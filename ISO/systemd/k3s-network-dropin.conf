[Unit]
# Live-boot: network-online.target never completes (dhcpcd, no
# networkd-wait-online), which would hold k3s's start job forever.
# Drop it — k3s binds 0.0.0.0 and is fine once network.target is up.
Wants=
After=network.target var-lib.mount
