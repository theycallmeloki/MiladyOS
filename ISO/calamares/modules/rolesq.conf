# MiladyOS role application job (exec phase, after unpackfs).
# Reads the packagechooser selection from global storage and applies it
# to the installed system: node.conf, join token, embedded payload.
---
payloadSource: "/run/live/medium/payload"
payloadTarget: "/opt/milady/payload"
nodeConf: "/etc/milady/node.conf"
