include menu.cfg
default vesamenu.c32
prompt 0
# Auto-boot the live entry after 5s (headless fleet nodes — no monitor).
# Kernel console=ttyS0 (from APPEND_LIVE) carries boot output to serial.
timeout 50
