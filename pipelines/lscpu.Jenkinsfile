pipeline {
    agent any
    stages {
        stage('CPU Stage') {
            steps {
                sh 'lscpu'
                sh 'env'

                // Other CPU dependent tasks
            }
        }
    }
}
