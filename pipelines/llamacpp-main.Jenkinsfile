pipeline {
    agent any
    stages {
        stage('Built Llama cpp') {
            steps {
                sh '/llamacpp/main -h'
                // Other CPU dependent tasks
            }
        }
    }
}