apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-tools
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/theycallmeloki/MiladyOS
    targetRevision: HEAD
    path: deploy/mcp-tools
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-tools
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
