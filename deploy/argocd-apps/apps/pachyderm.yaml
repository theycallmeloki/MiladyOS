apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pachyderm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/theycallmeloki/MiladyOS
    targetRevision: HEAD
    path: deploy/pachyderm
  destination:
    server: https://kubernetes.default.svc
    namespace: pachyderm
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
