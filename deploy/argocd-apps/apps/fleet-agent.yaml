apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fleet-agent
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/theycallmeloki/MiladyOS
    targetRevision: HEAD
    path: deploy/fleet-agent
  destination:
    server: https://kubernetes.default.svc
    namespace: fleet-agent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
