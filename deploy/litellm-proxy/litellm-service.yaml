apiVersion: v1
kind: Service
metadata:
  name: litellm-proxy
  namespace: default
  labels:
    app: litellm-proxy
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: litellm-proxy