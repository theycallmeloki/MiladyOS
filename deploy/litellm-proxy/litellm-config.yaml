apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: default
data:
  config.yaml: |
    model_list:
      # ── Free OpenRouter models with tool-calling support ──────────────────
      # Daisy-chained: rate-limit or failure on one → LiteLLM tries the next.
      # Only models confirmed to support the tools API are included.

      # Tier 1: confirmed working with tool calling
      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/nvidia/nemotron-3-nano-30b-a3b:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "nemotron-30b-free"

      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/upstage/solar-pro-3:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "solar-pro-3-free"

      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/arcee-ai/trinity-large-preview:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "trinity-large-free"

      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/stepfun/step-3.5-flash:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "step-3-5-flash-free"

      # Tier 2: work once "Allow training on prompts" propagates in OpenRouter
      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/google/gemma-3-27b-it:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "gemma3-27b-free"

      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/openai/gpt-oss-20b:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "gpt-oss-20b-free"

      - model_name: "fleet-agent"
        litellm_params:
          model: "openrouter/openai/gpt-oss-120b:free"
          api_key: "os.environ/OPENROUTER_API_KEY"
        model_info:
          id: "gpt-oss-120b-free"

      # ── Dedicated inference (fallbacks) ───────────────────────────────────
      # Bare-metal RTX 3080 Ti (AWQ 4-bit, low latency, no cloud dependency)
      - model_name: "Nanbeige/Nanbeige4.1-3B"
        litellm_params:
          model: "openai/cyankiwi/Nanbeige4.1-3B-AWQ-4bit"
          api_base: "http://192.168.1.19:8000/v1"
          api_key: "not-needed"
        model_info:
          id: "nanbeige-bare-metal"

      # In-cluster vLLM (last resort)
      - model_name: "Nanbeige/Nanbeige4.1-3B-fallback"
        litellm_params:
          model: "openai/Nanbeige/Nanbeige4.1-3B"
          api_base: "http://vllm-server.self-hosted-models.svc.cluster.local:8080/v1"
          api_key: "not-needed"
        model_info:
          id: "nanbeige-incluster"

    litellm_settings:
      drop_params: true
      telemetry: false
      callbacks: "custom_callbacks.proxy_handler_instance"

    general_settings:
      health_check_interval: 0   # disable background health-check model calls

    router_settings:
      num_retries: 1
      timeout: 60
      routing_strategy: "usage-based-routing-v2"
      allowed_fails: 1          # mark model unhealthy after 1 failure
      cooldown_time: 120        # seconds before retrying a failed model
      fallbacks:
        - "fleet-agent": ["Nanbeige/Nanbeige4.1-3B", "Nanbeige/Nanbeige4.1-3B-fallback"]
        - "Nanbeige/Nanbeige4.1-3B": ["Nanbeige/Nanbeige4.1-3B-fallback"]
