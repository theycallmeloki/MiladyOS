apiVersion: v1
kind: Service
metadata:
  name: mcp-tools
  namespace: mcp-tools
  labels:
    app: mcp-tools
spec:
  selector:
    app: mcp-tools
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  type: ClusterIP
