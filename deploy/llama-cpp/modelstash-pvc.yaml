apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: modelstash-storage
  namespace: llama-cpp
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
