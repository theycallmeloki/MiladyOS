apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-server
  namespace: llama-cpp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llama-server
  template:
    metadata:
      labels:
        app: llama-server
    spec:
      initContainers:
      - name: model-fetch
        image: busybox:stable
        command:
        - sh
        - -c
        - |
          if [ -f /models/Qwen3-Coder-Next-Q8_0-merged.gguf ]; then
            echo "Model already cached, skipping"
            exit 0
          fi
          rm -f /models/Qwen3-Coder-Next-Q8_0-merged.gguf.tmp
          echo "Downloading model from modelstash..."
          wget -O /models/Qwen3-Coder-Next-Q8_0-merged.gguf.tmp http://modelstash:8000/Qwen3-Coder-Next-Q8_0-merged.gguf && \
          mv /models/Qwen3-Coder-Next-Q8_0-merged.gguf.tmp /models/Qwen3-Coder-Next-Q8_0-merged.gguf
        volumeMounts:
        - name: model-cache
          mountPath: /models
      containers:
      - name: llama-server
        image: ogmiladyloki/llama-cpp-rpc-server:latest
        imagePullPolicy: Always
        command: ["llama-server"]
        args:
        - "-m"
        - "/models/Qwen3-Coder-Next-Q8_0-merged.gguf"
        - "--port"
        - "8080"
        - "-ngl"
        - "99"
        - "--rpc"
        - "rpc-worker-0.rpc-worker-headless.llama-cpp.svc.cluster.local:50052,rpc-worker-1.rpc-worker-headless.llama-cpp.svc.cluster.local:50052,rpc-worker-2.rpc-worker-headless.llama-cpp.svc.cluster.local:50052"
        ports:
        - containerPort: 8080
          name: http
        volumeMounts:
        - name: model-cache
          mountPath: /models
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 30
      volumes:
      - name: model-cache
        persistentVolumeClaim:
          claimName: model-cache
