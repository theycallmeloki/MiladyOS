apiVersion: v1
kind: Service
metadata:
  name: rpc-worker-headless
  namespace: llama-cpp
spec:
  clusterIP: None
  selector:
    app: rpc-worker
  ports:
  - port: 50052
    targetPort: 50052
    name: rpc
